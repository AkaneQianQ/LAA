---
phase: 08-mxu-assets-tasks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/py_service/pkg/server/websocket_server.py
  - agent/py_service/modules/auto_recognition/register.py
  - agent/py_service/modules/auto_recognition/window_detector.py
  - agent/py_service/modules/auto_recognition/hardware_scanner.py
  - agent/py_service/modules/auto_recognition/account_recognizer.py
  - agent/py_service/main.py
autonomous: true
requirements:
  - MXU-03
  - MXU-04
  - MXU-05
must_haves:
  truths:
    - "Python service exposes WebSocket API for frontend communication"
    - "Auto-recognition can detect Lost Ark game window automatically"
    - "Auto-recognition can scan and detect KMBox/Ferrum hardware on COM ports"
    - "Auto-recognition can identify account from first character screenshot"
  artifacts:
    - path: "agent/py_service/pkg/server/websocket_server.py"
      provides: "WebSocket server for frontend communication"
      exports: ["WebSocketServer", "start_server", "broadcast_log"]
    - path: "agent/py_service/modules/auto_recognition/window_detector.py"
      provides: "Game window auto-detection"
      exports: ["detect_game_window", "get_window_list"]
    - path: "agent/py_service/modules/auto_recognition/hardware_scanner.py"
      provides: "Hardware COM port scanning"
      exports: ["scan_serial_ports", "detect_kmbox_device"]
    - path: "agent/py_service/modules/auto_recognition/account_recognizer.py"
      provides: "Account identification from screenshot"
      exports: ["recognize_account", "get_account_identity"]
  key_links:
    - from: "agent/py_service/main.py"
      to: "agent/py_service/pkg/server/websocket_server.py"
      via: "import and start_server call"
      pattern: "from.*server.*websocket"
    - from: "agent/py_service/modules/auto_recognition"
      to: "agent/py_service/pkg/vision/engine.py"
      via: "VisionEngine for screenshot capture"
      pattern: "VisionEngine.*screenshot"
---

<objective>
Implement WebSocket server in Python service for frontend communication and create auto-recognition modules for game window, hardware, and account detection.

Purpose: Enable real-time communication between MXU frontend and Python backend, plus automatic detection of game state and hardware.
Output: WebSocket API with auto-recognition capabilities that the frontend can call via Tauri IPC.
</objective>

<execution_context>
@C:/Users/Akane/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-mxu-assets-tasks/08-CONTEXT.md
@agent/py_service/main.py
@agent/py_service/pkg/vision/engine.py
@agent/py_service/modules/character/detector.py

## Auto-Recognition Features Required
1. **Game Window Detection**: Find Lost Ark window by title/class name
2. **Hardware Detection**: Scan COM ports for KMBox/Ferrum device
3. **Account Recognition**: Use existing character detection to identify account

## WebSocket Protocol Design
Messages use JSON format:
- Request: {"type": "command", "action": "detect_window", "id": "uuid"}
- Response: {"type": "response", "id": "uuid", "data": {...}}
- Event: {"type": "event", "event": "log", "data": {"message": "...", "level": "info"}}

## Existing Components to Reuse
- VisionEngine: For screenshot capture and template matching
- CharacterDetector: For account identification via first character
- FerrumController: For hardware connection testing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WebSocket Server Module</name>
  <files>agent/py_service/pkg/server/websocket_server.py, agent/py_service/pkg/server/__init__.py</files>
  <action>
Create WebSocket server for frontend-backend communication using asyncio and websockets library.

1. Create agent/py_service/pkg/server/__init__.py package file

2. Create agent/py_service/pkg/server/websocket_server.py with:

   a. WebSocketServer class:
      - __init__(host="127.0.0.1", port=8765)
      - start() - Start the server
      - stop() - Stop the server gracefully
      - register_handler(command, handler_func) - Register command handlers
      - broadcast(event_type, data) - Broadcast events to all connected clients

   b. Message protocol:
      - Command messages: {"type": "command", "action": "...", "params": {}, "id": "uuid"}
      - Response messages: {"type": "response", "id": "uuid", "status": "ok|error", "data": {}}
      - Event messages: {"type": "event", "event": "...", "data": {}}

   c. Built-in handlers:
      - "ping" -> returns pong
      - "get_status" -> returns service status
      - "get_tasks" -> returns task list from interface.json
      - "start_task" -> starts a task execution
      - "stop_task" -> stops current task

   d. Connection management:
      - Track connected clients
      - Handle disconnections gracefully
      - Log connections with [WebSocket] prefix

3. Add websockets to project dependencies (if not already present)

4. Create helper function broadcast_log(message, level="info") for streaming logs to frontend

Use asyncio for async operation. Handle JSON decode errors gracefully.
  </action>
  <verify>
    <automated>cd C:/Users/Akane/Desktop/IDE/Project/FerrumProject/LostarkGuildDonationBot && python -c "from agent.py_service.pkg.server.websocket_server import WebSocketServer; print('[OK] WebSocketServer imported successfully')"</automated>
  </verify>
  <done>
    - WebSocketServer class can be imported without errors
    - Server starts and listens on configured port
    - broadcast_log function available for log streaming
    - Command handler registration works
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Auto-Recognition Modules</name>
  <files>agent/py_service/modules/auto_recognition/window_detector.py, agent/py_service/modules/auto_recognition/hardware_scanner.py, agent/py_service/modules/auto_recognition/account_recognizer.py, agent/py_service/modules/auto_recognition/__init__.py, agent/py_service/modules/auto_recognition/register.py</files>
  <action>
Create auto-recognition modules for game window, hardware, and account detection.

1. Create agent/py_service/modules/auto_recognition/ directory structure

2. Create window_detector.py:
   - detect_game_window() -> Find Lost Ark window by title patterns
   - get_window_list() -> List all windows with titles
   - WindowInfo dataclass with hwnd, title, rect, is_foreground
   - Support Korean title: "LOST ARK" or "로스트아크"
   - Use win32gui on Windows platform
   - Return window bounding box for screenshot ROI

3. Create hardware_scanner.py:
   - scan_serial_ports() -> List available COM ports
   - detect_kmbox_device(port) -> Test if device responds to km.init()
   - auto_detect_hardware() -> Scan ports and return first working device
   - Default to COM2, fallback to COM3, COM4, etc.
   - Return HardwareInfo with port, baudrate, device_type
   - Use pyserial for port scanning

4. Create account_recognizer.py:
   - recognize_account(screenshot) -> Identify account from character
   - Uses existing character detection from modules/character/detector.py
   - Capture first character slot screenshot
   - Generate perceptual hash for account identity
   - Return AccountInfo with hash, character_name (if detectable), confidence
   - Reuse existing account indexing logic

5. Create register.py with recognition and action decorators:
   - @recognition("GameWindowDetection") -> window detection
   - @recognition("HardwareDetection") -> hardware scanning
   - @recognition("AccountRecognition") -> account identification
   - def register(): print("[Module] auto_recognition registered")

All modules use Chinese log prefixes per project convention:
- [窗口] for window detection
- [硬件] for hardware scanning
- [账号] for account recognition
  </action>
  <verify>
    <automated>cd C:/Users/Akane/Desktop/IDE/Project/FerrumProject/LostarkGuildDonationBot && python -c "from agent.py_service.modules.auto_recognition.window_detector import detect_game_window; from agent.py_service.modules.auto_recognition.hardware_scanner import scan_serial_ports; from agent.py_service.modules.auto_recognition.account_recognizer import recognize_account; print('[OK] All auto-recognition modules imported')"</automated>
  </verify>
  <done>
    - window_detector.py exports detect_game_window and get_window_list
    - hardware_scanner.py exports scan_serial_ports and detect_kmbox_device
    - account_recognizer.py exports recognize_account
    - All modules follow project logging conventions
    - register.py properly registers all recognitions
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate WebSocket and Auto-Recognition into Main Service</name>
  <files>agent/py_service/main.py</files>
  <action>
Integrate WebSocket server and auto-recognition into the main Python service.

1. Modify agent/py_service/main.py:

   a. Add imports:
      - from agent.py_service.pkg.server.websocket_server import WebSocketServer, broadcast_log
      - from agent.py_service.modules.auto_recognition.window_detector import detect_game_window
      - from agent.py_service.modules.auto_recognition.hardware_scanner import auto_detect_hardware
      - from agent.py_service.modules.auto_recognition.account_recognizer import recognize_account

   b. Add new command-line arguments:
      - --server-mode: Run in WebSocket server mode (no single task)
      - --ws-port: WebSocket server port (default: 8765)
      - --auto-detect: Run auto-detection on startup

   c. Register WebSocket command handlers in initialize():
      - "detect_window" -> detect_game_window handler
      - "detect_hardware" -> auto_detect_hardware handler
      - "recognize_account" -> recognize_account handler
      - "get_task_config" -> return task configuration

   d. Add server mode execution:
      - If --server-mode: start WebSocket server and keep running
      - Integrate with existing initialization flow
      - Log "[服务] WebSocket 服务器已启动于 ws://127.0.0.1:8765"

   e. Add auto-detection on startup (when --auto-detect):
      - Detect game window
      - Detect hardware
      - Log results with [自动识别] prefix

2. Ensure graceful shutdown:
   - Handle Ctrl+C to stop WebSocket server
   - Clean up hardware connections
   - Log shutdown messages

3. Update run_task() to broadcast progress via WebSocket:
   - Send events for: task_start, step_progress, task_complete, task_error
   - Use broadcast_log for execution logs
  </action>
  <verify>
    <automated>cd C:/Users/Akane/Desktop/IDE/Project/FerrumProject/LostarkGuildDonationBot && python -m agent.py_service.main --help | grep -E "(server-mode|ws-port|auto-detect)"</automated>
  </verify>
  <done>
    - main.py accepts --server-mode, --ws-port, --auto-detect arguments
    - WebSocket server starts and registers all auto-recognition handlers
    - Service can run in both single-task mode and server mode
    - Auto-detection runs when --auto-detect flag is present
  </done>
</task>

</tasks>

<verification>
1. WebSocket server starts and accepts connections
2. All auto-recognition handlers respond to commands
3. Game window detection finds Lost Ark window
4. Hardware scanner detects KMBox on available COM ports
5. Account recognizer generates identity from character screenshot
6. Service runs in both single-task and server modes
</verification>

<success_criteria>
- WebSocket server listens on configured port (default 8765)
- Frontend can connect and send commands
- detect_game_window returns window info for Lost Ark
- auto_detect_hardware finds KMBox device
- recognize_account generates account hash from character
- All commands return properly formatted JSON responses
- Log streaming works via broadcast_log
</success_criteria>

<output>
After completion, create `.planning/phases/08-mxu-assets-tasks/08-02-SUMMARY.md`
</output>
