---
phase: 02-configuration-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflow_schema.py
  - core/config_loader.py
  - core/workflow_compiler.py
  - tests/config_system/test_config_loader_and_schema.py
autonomous: true
requirements:
  - CFG-01
  - CFG-03
must_haves:
  truths:
    - "Invalid workflow YAML is rejected before automation starts."
    - "Workflow steps are loaded from a single YAML file with required step_id for each step."
    - "Action definitions for click, wait, press, and scroll are validated with strict field rules."
  artifacts:
    - path: "core/workflow_schema.py"
      provides: "Strict typed models for workflow, steps, actions, and branching metadata"
      exports: ["WorkflowConfig", "WorkflowStep", "ActionConfig"]
    - path: "core/config_loader.py"
      provides: "YAML-only config loading entrypoint"
      exports: ["load_workflow_config"]
    - path: "core/workflow_compiler.py"
      provides: "Semantic compile-time validation for step graph references"
      exports: ["compile_workflow"]
    - path: "tests/config_system/test_config_loader_and_schema.py"
      provides: "Validation and loader regression coverage"
  key_links:
    - from: "core/config_loader.py"
      to: "core/workflow_schema.py"
      via: "model validation"
      pattern: "WorkflowConfig\.model_validate"
    - from: "core/config_loader.py"
      to: "core/workflow_compiler.py"
      via: "compile call on loaded model"
      pattern: "compile_workflow\("
---

<objective>
Create the strict YAML configuration foundation: parse, validate, and compile workflow definitions before runtime execution.

Purpose: Guarantee fail-fast startup validation and enforce the locked configuration contract.
Output: Loader + schema + compiler modules with tests proving strict validation behavior.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-system/02-CONTEXT.md
@.planning/phases/02-configuration-system/02-RESEARCH.md
@core/vision_engine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict workflow schema contracts and failing tests</name>
  <files>core/workflow_schema.py, tests/config_system/test_config_loader_and_schema.py</files>
  <behavior>
    - Test: missing step_id in any step fails validation.
    - Test: unsupported action type fails validation.
    - Test: wait action accepts only integer milliseconds field.
    - Test: click, press, scroll require locked parameter conventions.
  </behavior>
  <action>Create Pydantic v2 models for workflow config, including step-level IDs, explicit next links, conditional branch fields, and discriminated action models for click/wait/press/scroll. First add failing tests, then implement the minimal schema logic to pass.</action>
  <verify>
    <automated>pytest tests/config_system/test_config_loader_and_schema.py -x</automated>
  </verify>
  <done>Schema models enforce locked field and action constraints; tests pass and failures are descriptive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement YAML-only loader and compile-time semantic validation</name>
  <files>core/config_loader.py, core/workflow_compiler.py, tests/config_system/test_config_loader_and_schema.py</files>
  <behavior>
    - Test: non-YAML extension is rejected.
    - Test: loader uses safe parsing and rejects malformed YAML.
    - Test: dangling next/on_true/on_false references fail compile.
    - Test: valid workflow returns compiled object ready for executor.
  </behavior>
  <action>Implement `load_workflow_config(path)` to read one YAML file via `yaml.safe_load`, validate against schema, and run `compile_workflow` for semantic graph checks (reference existence, unique step_id, start step presence). Keep startup fail-fast behavior: any validation error blocks execution.</action>
  <verify>
    <automated>pytest tests/config_system/test_config_loader_and_schema.py -x</automated>
  </verify>
  <done>Single-file YAML workflow loading is deterministic and rejects invalid configs before runtime.</done>
</task>

</tasks>

<verification>
- Schema and loader test suite passes with both valid and invalid fixture cases.
- No runtime execution starts when parsing, structural, or graph validation fails.
</verification>

<success_criteria>
1. YAML workflow file is loaded through a single public loader function.
2. Every step requires explicit `step_id` and action payload conforms to strict schema.
3. Invalid references or malformed action parameters are rejected at startup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-system/02-01-SUMMARY.md`
</output>
