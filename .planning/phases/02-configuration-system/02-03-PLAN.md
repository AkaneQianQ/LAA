---
phase: 02-configuration-system
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - config/workflows/guild_donation.yaml
  - gui_launcher.py
  - tests/config_system/test_workflow_integration.py
  - core/workflow_bootstrap.py
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
must_haves:
  truths:
    - "A complete guild donation workflow can be defined in one YAML file and loaded by the launcher."
    - "Launcher can initialize workflow execution from config without manual code edits for step logic."
    - "End-to-end integration test proves config load, compile, and execute pipeline wiring."
  artifacts:
    - path: "config/workflows/guild_donation.yaml"
      provides: "Reference single-file workflow using all required action types and conditions"
    - path: "core/workflow_bootstrap.py"
      provides: "Bootstrap entrypoint to load compiled workflow and create executor"
      exports: ["create_workflow_executor"]
    - path: "gui_launcher.py"
      provides: "Integration point invoking workflow bootstrap in automation path"
    - path: "tests/config_system/test_workflow_integration.py"
      provides: "Pipeline integration verification"
  key_links:
    - from: "gui_launcher.py"
      to: "core/workflow_bootstrap.py"
      via: "automation start path"
      pattern: "create_workflow_executor"
    - from: "core/workflow_bootstrap.py"
      to: "core/config_loader.py"
      via: "load config file"
      pattern: "load_workflow_config"
    - from: "core/workflow_bootstrap.py"
      to: "core/workflow_executor.py"
      via: "executor construction"
      pattern: "WorkflowExecutor"
---

<objective>
Wire the configuration engine into runtime entry flow and provide a concrete YAML guild donation workflow reference.

Purpose: Ensure the phase ships as a usable, end-to-end configuration system instead of isolated modules.
Output: Launcher bootstrap integration, sample workflow YAML, and integration tests.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-system/02-CONTEXT.md
@gui_launcher.py
@core/config_loader.py
@core/workflow_executor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add end-to-end integration tests and workflow bootstrap module</name>
  <files>tests/config_system/test_workflow_integration.py, core/workflow_bootstrap.py, config/workflows/guild_donation.yaml</files>
  <behavior>
    - Test: sample YAML loads and compiles into executable workflow.
    - Test: executor created by bootstrap can run through mocked dispatcher/vision dependencies.
    - Test: sample workflow includes click, wait, press, scroll, and at least one image-based branch.
  </behavior>
  <action>Create a bootstrap helper that builds executor from workflow path and runtime dependencies. Add a realistic single-file `guild_donation.yaml` that demonstrates explicit step IDs, next links, condition branch, and loop path. Write tests first, then implement bootstrap and sample config updates required for passing coverage.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_integration.py -x</automated>
  </verify>
  <done>Bootstrap module and sample workflow are valid and executable in integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate workflow bootstrap into launcher automation path</name>
  <files>gui_launcher.py, tests/config_system/test_workflow_integration.py</files>
  <action>Update launcher automation startup path to construct and trigger config workflow execution using `core/workflow_bootstrap.py`, while preserving existing stop-event behavior and thread lifecycle control. Keep a clear fallback log if config load/validation fails.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_integration.py -x</automated>
  </verify>
  <done>Launcher can start automation from YAML workflow definition and reports startup validation failures cleanly.</done>
</task>

</tasks>

<verification>
- Integration test verifies full parse->validate->compile->execute wiring from sample YAML.
- Launcher path uses workflow bootstrap and does not bypass strict startup validation.
</verification>

<success_criteria>
1. Example guild donation workflow exists as a single YAML file.
2. Launcher delegates automation orchestration to config system path.
3. End-to-end integration test passes for config-driven workflow execution.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-system/02-03-SUMMARY.md`
</output>
