---
phase: 01-character-detection-core
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - modules/character_detector.py
  - core/database.py
  - gui_launcher.py
  - tests/character_detection/test_account_indexing.py
autonomous: true
requirements:
  - CHAR-04
  - CHAR-05
must_haves:
  truths:
    - "First detected character is captured and stored as account index on first encounter."
    - "Account identity is derived from first-character screenshot hash and timestamp metadata."
    - "Character screenshots are cached per account and reused for future recognition."
  artifacts:
    - path: "modules/character_detector.py"
      provides: "Index-capture and cache-or-load orchestration"
    - path: "core/database.py"
      provides: "Account lookup and character screenshot metadata persistence"
    - path: "tests/character_detection/test_account_indexing.py"
      provides: "Automated tests for hash identity and cache reuse behavior"
  key_links:
    - from: "modules/character_detector.py"
      to: "data/accounts/{account_hash}/characters/{slot_index}.png"
      via: "filesystem write/read of screenshots"
      pattern: "data/accounts/.*/characters"
    - from: "modules/character_detector.py"
      to: "core/database.py"
      via: "account hash lookup and character metadata upsert"
      pattern: "find_account|create_account|upsert_character"
    - from: "gui_launcher.py"
      to: "modules/character_detector.py"
      via: "first-run discovery call before main automation loop"
      pattern: "CharacterDetector|discover|index"
---

<objective>
Implement account indexing and screenshot cache flow for first-run and repeat-run behavior.

Purpose: Deliver zero-config account recognition and persistent character cache for later phases.
Output: First-character index capture, account hash persistence, and cache-driven recognition path.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-character-detection-core/01-CONTEXT.md
@.planning/phases/01-character-detection-core/01-RESEARCH.md
@.planning/phases/01-character-detection-core/01-01-SUMMARY.md
@.planning/phases/01-character-detection-core/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement quick index mode with first-character screenshot hashing</name>
  <files>modules/character_detector.py, core/database.py</files>
  <behavior>
    - First run captures first detected character screenshot only for account indexing.
    - Account hash is deterministic from screenshot bytes (SHA-256) plus stored creation timestamp.
    - Account record is created exactly once for same screenshot identity.
  </behavior>
  <action>Add `create_or_get_account_index()` flow that captures first occupied slot screenshot, computes hash, creates account directory structure, and writes metadata to SQLite.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_account_indexing.py::test_first_character_creates_account_index -q</automated>
  </verify>
  <done>First-run path creates stable account identity without OCR or manual input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cache individual character screenshots and support repeat-run recognition</name>
  <files>modules/character_detector.py, core/database.py, tests/character_detection/test_account_indexing.py</files>
  <behavior>
    - Screenshots are stored under `data/accounts/{account_hash}/characters/{slot_index}.png`.
    - Metadata links each slot index to image path and last_seen timestamp.
    - Repeat run resolves account from first-character hash and loads cached character set.
  </behavior>
  <action>Implement screenshot save/update and cache load methods, including idempotent upsert behavior for slot records and existence checks for stale files.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_account_indexing.py::test_character_cache_reused_on_subsequent_runs -q</automated>
  </verify>
  <done>Cache files + metadata persist and are reused on later runs for quick recognition.</done>
</task>

<task type="auto">
  <name>Task 3: Wire first-run discovery/index step into launcher flow</name>
  <files>gui_launcher.py</files>
  <action>Integrate phase-1 discovery entrypoint so launcher invokes account recognition/indexing before main automation starts. Preserve existing threading/stop-event patterns and user-facing message style.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_account_indexing.py -q</automated>
  </verify>
  <done>Launcher workflow includes account identification bootstrap and continues into automation pipeline.</done>
</task>

</tasks>

<verification>
Run account-indexing tests to validate first-run index creation, cache persistence, and repeat-run recognition behavior.
</verification>

<success_criteria>
- First character screenshot is captured and used as account index key.
- Account/character screenshot cache is persisted in SQLite + filesystem using locked directory structure.
- Subsequent runs can resolve account from cached screenshot identity without manual character count.
</success_criteria>

<output>
After completion, create `.planning/phases/01-character-detection-core/01-03-SUMMARY.md`
</output>

