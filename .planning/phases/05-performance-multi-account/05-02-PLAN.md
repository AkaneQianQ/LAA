---
phase: 05-performance-multi-account
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/parallel_matcher.py
  - modules/character_detector.py
  - tests/performance/test_parallel_matching.py
  - tests/performance/test_roi_constraints.py
autonomous: true
requirements:
  - SPEED-01
  - SPEED-02
must_haves:
  truths:
    - Parallel ROI matching completes faster than sequential for 9 slots
    - All template matching uses ROI constraints (no full-screen matching)
    - ThreadPoolExecutor properly manages worker threads
    - CharacterDetector uses parallel matching for slot scanning
  artifacts:
    - path: core/parallel_matcher.py
      provides: Parallel template matching across multiple ROIs
      exports: [ParallelMatcher, match_single_roi, scan_rois_parallel]
    - path: tests/performance/test_parallel_matching.py
      provides: Performance benchmarks for parallel vs sequential
      min_tests: 4
    - path: tests/performance/test_roi_constraints.py
      provides: Verification that all matching uses ROIs
      min_tests: 3
  key_links:
    - from: modules/character_detector.py
      to: core/parallel_matcher.py
      via: CharacterDetector.scan_visible_slots_parallel()
      pattern: ParallelMatcher.scan_slots()
---

<objective>
Implement parallel ROI template matching to achieve <100ms per ROI-constrained search and <200ms total for 9-slot scanning.

Purpose: Sequential slot scanning is slow (9 slots × ~50ms = 450ms). Parallel processing with ThreadPoolExecutor can reduce this to <200ms.
Output: ParallelMatcher class integrated into CharacterDetector with ROI constraint enforcement.
</objective>

<execution_context>
@C:/Users/Akane/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-performance-multi-account/05-RESEARCH.md
@core/vision_engine.py
@modules/character_detector.py

## Research Patterns

From 05-RESEARCH.md, parallel ROI matching:
```python
def match_single_roi(screenshot, template, roi, slot_index):
    x1, y1, x2, y2 = roi
    roi_region = screenshot[y1:y2, x1:x2]
    if roi_region.size == 0:
        return slot_index, False, 0.0
    roi_gray = cv2.cvtColor(roi_region, cv2.COLOR_BGR2GRAY)
    result = cv2.matchTemplate(roi_gray, template, cv2.TM_CCOEFF_NORMED)
    _, max_val, _, _ = cv2.minMaxLoc(result)
    return slot_index, max_val >= 0.8, max_val

def scan_slots_parallel(screenshot, template, rois, max_workers=4):
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        futures = {
            executor.submit(match_single_roi, screenshot, template, roi, i): i
            for i, roi in enumerate(rois)
        }
        for future in as_completed(futures):
            slot_index, found, confidence = future.result()
            results[slot_index] = (found, confidence)
    return results
```

## Current CharacterDetector Structure

CharacterDetector currently has:
- `scan_visible_slots()` - sequential slot scanning
- `ALL_SLOT_ROIS` - list of 9 ROI tuples
- Uses `cv2.matchTemplate` with `TM_CCOEFF_NORMED`
- Threshold of 0.8 for detection

## ROI Constraint Requirements

SPEED-02 requires ALL template matching to use ROI constraints. Current violations to fix:
- vision_engine.py `find_element()` has optional roi parameter (can be None)
- Need to enforce ROI usage in all detection paths
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ParallelMatcher with ThreadPoolExecutor</name>
  <files>core/parallel_matcher.py</files>
  <behavior>
    - Test: match_single_roi returns correct slot_index and confidence
    - Test: scan_rois_parallel completes faster than sequential (benchmark)
    - Test: Results are correctly ordered by slot_index
    - Test: Empty ROI regions return (False, 0.0) without error
    - Test: ThreadPoolExecutor max_workers configurable (default 4)
  </behavior>
  <action>
Create `core/parallel_matcher.py` with parallel matching functionality:

1. **match_single_roi function**:
   - Args: screenshot (np.ndarray), template (np.ndarray), roi (Tuple[int,int,int,int]), slot_index (int)
   - Returns: Tuple[slot_index, found (bool), confidence (float)]
   - Extract ROI region: screenshot[y1:y2, x1:x2]
   - Handle empty ROI (return 0.0 confidence)
   - Convert to grayscale if needed
   - Use cv2.matchTemplate with TM_CCOEFF_NORMED
   - Return slot_index for result ordering

2. **ParallelMatcher class**:
   - Constructor: `max_workers: int = 4` (configurable)
   - Method: `scan_rois(screenshot, template, rois) -> Dict[int, Tuple[bool, float]]`
   - Uses ThreadPoolExecutor for concurrent execution
   - Collects results keyed by slot_index
   - Returns dict: {slot_index: (found, confidence)}

3. **Performance optimization**:
   - Use as_completed for result collection
   - Handle exceptions gracefully (log error, return False for that slot)
   - Clean thread pool shutdown via context manager

4. **Benchmark utility**:
   - Function: `benchmark_parallel_vs_sequential()` for testing speedup
   - Returns timing comparison dict

5. **Type hints**: Full typing throughout

Note: OpenCV releases GIL during matchTemplate, so threads provide actual speedup.
  </action>
  <verify>
    <automated>pytest tests/performance/test_parallel_matching.py -x -v</automated>
  </verify>
  <done>ParallelMatcher class exists, all unit tests pass, benchmark shows speedup</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parallel slot scanning to CharacterDetector</name>
  <files>modules/character_detector.py</files>
  <behavior>
    - Test: scan_visible_slots_parallel returns same results as sequential
    - Test: Parallel version is measurably faster
    - Test: SlotOccupancyResult objects correctly constructed
    - Test: Retry logic still works with parallel results
  </behavior>
  <action>
Extend CharacterDetector with parallel scanning:

1. **Add scan_visible_slots_parallel method**:
   - Same signature as scan_visible_slots but uses ParallelMatcher
   - Returns List[SlotOccupancyResult] (same format)
   - Use ParallelMatcher to scan all 9 ROIs concurrently
   - Construct SlotOccupancyResult for each slot
   - Maintain same retry logic and threshold behavior

2. **Integration approach**:
   - Add optional `use_parallel: bool = False` parameter to __init__
   - Store ParallelMatcher instance if parallel enabled
   - Keep sequential method for backward compatibility
   - Allow runtime switching between modes

3. **Preserve existing behavior**:
   - Same threshold (SLOT_DETECTION_THRESHOLD = 0.8)
   - Same retry logic for borderline cases
   - Same SlotOccupancyResult format
   - Same template loading via _load_template()

4. **Optimization**:
   - Load template once before parallel scan
   - Pass pre-loaded template to ParallelMatcher
   - Avoid redundant template loading per slot

5. **Update imports**:
   - Import ParallelMatcher from core.parallel_matcher
   - Import Dict from typing if not already present

Ensure the parallel version produces identical results to sequential (deterministic matching).
  </action>
  <verify>
    <automated>pytest tests/performance/test_parallel_matching.py::test_character_detector_parallel -x -v</automated>
  </verify>
  <done>CharacterDetector has scan_visible_slots_parallel(), produces identical results, faster execution</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce ROI constraints in all matching paths</name>
  <files>core/vision_engine.py, tests/performance/test_roi_constraints.py</files>
  <behavior>
    - Test: find_element requires ROI parameter (no None default)
    - Test: match_template_roi handles all edge cases
    - Test: All CharacterDetector methods use ROI constraints
    - Test: Full-screen matching raises ValueError or uses default ROI
  </behavior>
  <action>
Enforce SPEED-02 ROI constraints across the codebase:

1. **Update vision_engine.py find_element**:
   - Change `roi: Optional[Tuple[int, int, int, int]] = None` to required parameter
   - Remove full-screen fallback path
   - Raise ValueError if roi is None or invalid
   - Update docstring to reflect ROI requirement

2. **Update vision_engine.py match_template_roi**:
   - Add validation: roi must be 4-tuple with x1 < x2 and y1 < y2
   - Add validation: ROI must be within screenshot bounds
   - Return (0.0, (0, 0)) for out-of-bounds (existing behavior)

3. **Audit CharacterDetector**:
   - Verify ALL_SLOT_ROIs are used in all detection methods
   - Ensure detect_character_selection_screen uses SLOT_1_1_ROI
   - Ensure detect_scroll_bottom uses SCROLLBAR_BOTTOM_ROI
   - No implicit full-screen matching

4. **Create test_roi_constraints.py**:
   - Test that find_element requires ROI
   - Test that invalid ROI raises appropriate error
   - Test that all CharacterDetector methods use constrained ROIs
   - Test ROI bounds validation

5. **Backward compatibility note**:
   - This is a breaking change for any code calling find_element without ROI
   - Update any internal callers to provide explicit ROIs
   - Document the change in comments

The goal is to ensure no full-screen template matching occurs anywhere in the codebase.
  </action>
  <verify>
    <automated>pytest tests/performance/test_roi_constraints.py -x -v</automated>
  </verify>
  <done>All template matching uses ROI constraints, tests verify no full-screen matching paths exist</done>
</task>

</tasks>

<verification>
- [ ] ParallelMatcher class exists with ThreadPoolExecutor
- [ ] Benchmark shows parallel faster than sequential for 9 slots
- [ ] CharacterDetector has parallel scanning method
- [ ] ROI constraints enforced (no full-screen matching)
- [ ] All tests pass: pytest tests/performance/test_parallel_matching.py
- [ ] All tests pass: pytest tests/performance/test_roi_constraints.py
</verification>

<success_criteria>
1. Parallel slot scan completes in <200ms (vs ~450ms sequential)
2. Results identical between parallel and sequential modes
3. All template matching uses explicit ROI constraints
4. ThreadPoolExecutor properly manages resources (no leaks)
5. All unit tests pass with > 90% coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-multi-account/05-02-SUMMARY.md`
</output>
