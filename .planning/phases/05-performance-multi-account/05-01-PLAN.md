---
phase: 05-performance-multi-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/frame_cache.py
  - core/vision_engine.py
  - tests/performance/test_frame_cache.py
  - tests/performance/conftest.py
autonomous: true
requirements:
  - SPEED-04
must_haves:
  truths:
    - Frame cache reduces screen capture calls by 50% or more
    - Cache invalidates automatically after TTL expiration
    - Cache provides thread-safe access for concurrent operations
    - Vision engine integrates cache without breaking existing API
  artifacts:
    - path: core/frame_cache.py
      provides: FrameCache class with TTL-based invalidation
      exports: [FrameCache]
    - path: tests/performance/test_frame_cache.py
      provides: Unit tests for cache behavior
      min_tests: 6
  key_links:
    - from: core/vision_engine.py
      to: core/frame_cache.py
      via: VisionEngine uses FrameCache for screenshot caching
      pattern: self._frame_cache.get() / set()
---

<objective>
Implement a thread-safe frame caching system with TTL-based invalidation to reduce DXCam screen capture overhead by 50% or more.

Purpose: Screen capture is expensive (50-100ms per call). Multiple template matches within a short time window can reuse the same frame, significantly improving performance.
Output: FrameCache class integrated into VisionEngine with comprehensive tests.
</objective>

<execution_context>
@C:/Users/Akane/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-performance-multi-account/05-RESEARCH.md
@core/vision_engine.py

## Research Patterns

From 05-RESEARCH.md, the FrameCache pattern:
```python
class FrameCache:
    def __init__(self, ttl_ms: float = 100.0):
        self.ttl_ms = ttl_ms
        self._cache: Optional[np.ndarray] = None
        self._timestamp: float = 0.0
        self._lock = threading.Lock()

    def get(self) -> Optional[np.ndarray]:
        with self._lock:
            if self._cache is None:
                return None
            elapsed_ms = (time.monotonic() - self._timestamp) * 1000
            if elapsed_ms > self._ttl_ms:
                self._cache = None
                return None
            return self._cache.copy()

    def set(self, frame: np.ndarray) -> None:
        with self._lock:
            self._cache = frame.copy()
            self._timestamp = time.monotonic()
```

## Current VisionEngine Structure

VisionEngine currently has:
- `_template_cache: dict` for caching loaded templates
- `find_element()` method for template matching
- No frame caching (captures fresh screenshot every time)

The FrameCache will be added as a new component that VisionEngine can optionally use.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FrameCache class with TTL invalidation</name>
  <files>core/frame_cache.py</files>
  <behavior>
    - Test: Cache stores and retrieves frame within TTL
    - Test: Cache returns None after TTL expires
    - Test: Cache returns copy to prevent mutation
    - Test: Thread-safe concurrent access
    - Test: Cache stats track hits/misses
    - Test: Explicit invalidate() clears cache
  </behavior>
  <action>
Create `core/frame_cache.py` with FrameCache class:

1. **Constructor**: Accept `ttl_ms: float = 150.0` (configurable, default 150ms based on research recommendation)

2. **get() method**:
   - Thread-safe with threading.Lock()
   - Returns cached frame copy if not expired
   - Returns None if expired or no cache
   - Uses time.monotonic() for clock skew safety

3. **set() method**:
   - Thread-safe with threading.Lock()
   - Stores frame.copy() to prevent external mutation
   - Updates timestamp

4. **invalidate() method**:
   - Force cache clear for action-after scenarios
   - Thread-safe

5. **Stats tracking**:
   - `_capture_count`: Number of fresh captures
   - `_cache_hit_count`: Number of cache hits
   - `cache_stats` property returning dict with captures, hits, hit_rate, reduction_pct

6. **Type hints**: Full typing with Optional[np.ndarray], float, etc.

Use numpy for frame storage. Ensure thread safety with proper lock usage.
  </action>
  <verify>
    <automated>pytest tests/performance/test_frame_cache.py -x -v</automated>
  </verify>
  <done>FrameCache class exists with all methods, passes all unit tests, thread-safe implementation verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate FrameCache into VisionEngine</name>
  <files>core/vision_engine.py</files>
  <behavior>
    - Test: VisionEngine accepts optional frame_cache parameter
    - Test: get_screenshot() uses cache when available
    - Test: get_screenshot(force_fresh=True) bypasses cache
    - Test: find_element() integrates with cached screenshots
    - Test: Cache stats accessible through VisionEngine
  </behavior>
  <action>
Extend VisionEngine to use FrameCache:

1. **Update __init__**:
   - Add optional `frame_cache: Optional[FrameCache] = None` parameter
   - Store as `self._frame_cache`

2. **Add get_screenshot() method**:
   - Accept `force_fresh: bool = False` parameter
   - Check cache first (if not force_fresh)
   - If cache miss, capture using DXCam pattern from research
   - Store in cache after capture
   - Return BGR frame (convert from RGB if needed)

3. **Add invalidate_cache() method**:
   - Delegate to frame_cache.invalidate() if present
   - No-op if no cache configured

4. **Preserve backward compatibility**:
   - Existing find_element() signature unchanged
   - Template cache behavior unchanged
   - Works without frame_cache (optional enhancement)

5. **Integration approach**:
   - FrameCache is dependency-injected, not hardcoded
   - Allows tests to use VisionEngine without cache
   - Allows runtime configuration of TTL

Note: Actual DXCam capture code should follow existing patterns in codebase. Use lazy import for dxcam to avoid import-time side effects.
  </action>
  <verify>
    <automated>pytest tests/performance/test_frame_cache.py::test_vision_engine_integration -x -v</automated>
  </verify>
  <done>VisionEngine accepts FrameCache, get_screenshot() uses cache, backward compatibility preserved</done>
</task>

<task type="auto">
  <name>Task 3: Create performance tests and conftest</name>
  <files>tests/performance/conftest.py, tests/performance/test_frame_cache.py</files>
  <action>
Create test infrastructure for performance tests:

1. **tests/performance/conftest.py**:
   - Shared fixtures for performance benchmarks
   - `sample_frame()` fixture: creates dummy 2560x1440 BGR frame
   - `frame_cache()` fixture: creates FrameCache with test TTL
   - `vision_engine_with_cache()` fixture: VisionEngine with FrameCache

2. **Complete test_frame_cache.py**:
   - Tests for FrameCache class (from Task 1)
   - Tests for VisionEngine integration (from Task 2)
   - Performance benchmark: verify cache hit rate > 50%
   - Thread safety test: concurrent get/set operations

3. **Test patterns**:
   - Use time.sleep() in tests to simulate TTL expiration
   - Use threading.Thread for concurrency tests
   - Use numpy array comparison for frame equality

4. **Test coverage**:
   - Cache hit scenario
   - Cache miss scenario
   - TTL expiration
   - Force fresh bypass
   - Thread safety (concurrent access)
   - Stats accuracy
   - Invalidate functionality

Ensure tests run in < 5 seconds total for fast feedback.
  </action>
  <verify>
    <automated>pytest tests/performance/test_frame_cache.py -x -v --tb=short</automated>
  </verify>
  <done>All 6+ tests pass, coverage includes TTL, threading, stats, and integration scenarios</done>
</task>

</tasks>

<verification>
- [ ] FrameCache class exists in core/frame_cache.py
- [ ] TTL-based invalidation works correctly (test with 50ms TTL)
- [ ] Thread safety verified with concurrent access test
- [ ] VisionEngine integrates FrameCache via dependency injection
- [ ] Cache stats report hit rate and reduction percentage
- [ ] All tests pass: pytest tests/performance/test_frame_cache.py
</verification>

<success_criteria>
1. Frame cache reduces screen capture calls by 50%+ (measured via stats)
2. TTL expiration works reliably (tested with short TTL)
3. Thread-safe concurrent access (no race conditions)
4. VisionEngine API backward compatible (existing code works unchanged)
5. All unit tests pass with > 90% coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-multi-account/05-01-SUMMARY.md`
</output>
