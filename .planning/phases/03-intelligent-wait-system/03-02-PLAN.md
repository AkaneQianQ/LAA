---
phase: 03-intelligent-wait-system
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - core/workflow_runtime.py
  - core/workflow_executor.py
  - tests/config_system/test_workflow_executor.py
autonomous: true
requirements:
  - WAIT-01
  - WAIT-02
  - WAIT-03
  - WAIT-04
must_haves:
  truths:
    - "Runtime waits until image appears before advancing when `state=appear`."
    - "Runtime waits until image disappears before advancing when `state=disappear`."
    - "Timeout produces step failure that is retried only through existing executor retry semantics."
    - "Retry attempts include configurable interval behavior."
  artifacts:
    - path: "core/workflow_runtime.py"
      provides: "Monotonic intelligent wait loop with 2-hit stability and polling."
    - path: "core/workflow_executor.py"
      provides: "Retry interval handling between retry attempts for failed steps."
    - path: "tests/config_system/test_workflow_executor.py"
      provides: "Behavior tests for appear/disappear waits, timeout, retry handoff, and retry interval."
  key_links:
    - from: "core/workflow_runtime.py"
      to: "core/vision_engine.py"
      via: "ConditionEvaluator image matching"
      pattern: "find_element"
    - from: "core/workflow_runtime.py"
      to: "core/workflow_executor.py"
      via: "raises ExecutionError on timeout"
      pattern: "ExecutionError"
    - from: "core/workflow_executor.py"
      to: "WorkflowStep.retry/retry_interval_ms"
      via: "attempt loop and sleep between retries"
      pattern: "retry|retry_interval"
---

<objective>
Implement intelligent wait execution semantics and wire timeout/retry behavior into the existing deterministic executor.

Purpose: Deliver runtime behavior for WAIT requirements without introducing a parallel retry system.
Output: State-driven wait loop with timeout, stability gating, and executor-owned retry interval logic.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligent-wait-system/03-CONTEXT.md
@.planning/phases/03-intelligent-wait-system/03-RESEARCH.md
@core/workflow_runtime.py
@core/workflow_executor.py
@tests/config_system/test_workflow_executor.py
@.planning/phases/03-intelligent-wait-system/03-01-SUMMARY.md

<interfaces>
From core/workflow_runtime.py:
```python
class ActionDispatcher:
    def dispatch(self, step: WorkflowStep) -> None: ...

class ConditionEvaluator:
    def evaluate(self, step: WorkflowStep, screenshot: Optional[Any] = None) -> bool: ...
```

From core/workflow_executor.py:
```python
class WorkflowExecutor:
    def _execute_step(self, step: WorkflowStep) -> tuple[bool, Optional[Exception]]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wait_image runtime loop for appear/disappear with stability guard</name>
  <files>core/workflow_runtime.py</files>
  <behavior>
    - Test 1: `state=appear` succeeds only after two consecutive positive image evaluations.
    - Test 2: `state=disappear` succeeds only after two consecutive negative image evaluations.
    - Test 3: timeout expiration raises execution failure.
  </behavior>
  <action>Extend action dispatch to handle `wait_image` by polling image condition with `time.monotonic()` deadline control. Enforce locked two-consecutive-hit stability before success. Resolve timeout/poll intervals through global defaults plus per-step/action override priority. On timeout, raise `ExecutionError` immediately (no internal retry loop) so executor remains retry owner.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_executor.py -k "wait_image or appear or disappear or timeout" -x</automated>
  </verify>
  <done>Runtime executes state-driven waits for both directions and fails deterministically on timeout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement retry interval semantics in executor retry loop</name>
  <files>core/workflow_executor.py, tests/config_system/test_workflow_executor.py</files>
  <behavior>
    - Test 1: failed step with retries waits configured interval before next attempt.
    - Test 2: max retries still terminates with final failure.
    - Test 3: timeout errors from wait_image are retried via existing `step.retry`.
  </behavior>
  <action>Update retry loop to respect `retry_interval_ms` (fixed short default with override) between retry attempts while keeping stop-on-failure baseline once attempts are exhausted. Ensure non-retriable exceptions preserve existing behavior. Add tests that prove timeout-triggered failures flow through the existing retry lifecycle instead of runtime-side retry duplication.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_executor.py -x</automated>
  </verify>
  <done>Executor applies configurable retry interval and remains the single retry authority for timeout failures.</done>
</task>

</tasks>

<verification>
Run executor/runtime test suite and confirm appear/disappear, timeout, and retry interval behaviors are covered with deterministic mocks.
</verification>

<success_criteria>
- `wait_image` runtime supports both appear and disappear states with 2-hit stability gating.
- Timeout and polling are configurable via defaults + overrides.
- Timeout triggers executor retry flow (`step.retry`) without introducing a second retry system.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligent-wait-system/03-02-SUMMARY.md`
</output>

