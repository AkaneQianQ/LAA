# Plan 07.02: Mouse Actions - Move, Click, and Scroll

**Phase:** 07-skills-ferrum
**Wave:** 1
**Depends On:** 07.01
**Estimated Duration:** 30-40 minutes

---

## Goal

Implement mouse control methods for FerrumController: move, click, and scroll. These
methods translate high-level actions into Ferrum KM API commands.

---

## Requirements Addressed

- REQ-MOUSE-01: Mouse movement via km.move(x, y)
- REQ-MOUSE-02: Left button click via km.click(0)
- REQ-MOUSE-03: Scroll wheel via km.wheel(amount)

---

## Implementation Details

### Ferrum KM API Reference

From documentation:

**km.move(x, y)** - Page 26
- x_amount: positive=right, negative=left
- y_amount: positive=down, negative=up
- Units are generic (OS-dependent, typically pixels)
- Both arguments required (use 0 for no movement in axis)

**km.click(button)** - Page 16
- 0 = left button
- 1 = right button
- 2 = middle (scroll wheel)
- Auto press delay: 75-125ms (random)
- Auto release delay: 125-175ms (random)
- Hardware override applies

**km.wheel(amount)** - Page 28
- positive = scroll up
- negative = scroll down
- Security: use only -1 or +1 (other values may look suspicious)

### Method Specifications

**click(x, y)**
1. Move to absolute position: `km.move(x, y)`
2. Click left button: `km.click(0)`
3. Return immediately (delays handled by Ferrum device)

**scroll(direction, ticks)**
1. Determine amount: +1 for "up", -1 for "down"
2. Loop ticks times, sending `km.wheel(amount)` each iteration
3. Brief delay between ticks (5ms) to avoid overwhelming device

---

## Tasks

<task id="1">
  <name>Implement move() helper method</name>
  <files>core/ferrum_controller.py</files>
  <action>Add internal _move() method that sends km.move(x, y) command. Note: km.move takes relative coordinates. For absolute positioning, the caller must track current position and calculate delta.</action>
  <verify>Command format matches Ferrum spec: _move(10, -5) sends "km.move(10, -5)"</verify>
  <done>_move() helper method implemented</done>
</task>

<task id="2">
  <name>Implement click() method</name>
  <files>core/ferrum_controller.py</files>
  <action>Add click(x: int, y: int) -> None method for ActionDispatcher: send km.move(x, y), then send km.click(0). Note: km.move is relative from current position.</action>
  <verify>Method signature matches ActionDispatcher expectation: click(100, 100) sends move then click commands</verify>
  <done>click() method implemented with correct signature</done>
</task>

<task id="3">
  <name>Implement scroll() method</name>
  <files>core/ferrum_controller.py</files>
  <action>Add scroll(direction: str, ticks: int) -> None method: determine amount (+1 for "up", -1 for "down"), loop ticks times sending km.wheel(amount) each iteration with 5ms delay between ticks</action>
  <verify>Direction mapping correct (up=+1, down=-1), test scroll("down", 3) sends km.wheel(-1) three times</verify>
  <done>scroll() method implemented</done>
</task>

<task id="4">
  <name>Add button constants</name>
  <files>core/ferrum_controller.py</files>
  <action>Define button constants in class: BUTTON_LEFT = 0, BUTTON_RIGHT = 1, BUTTON_MIDDLE = 2, BUTTON_SIDE_REAR = 3, BUTTON_SIDE_FRONT = 4</action>
  <verify>Constants match Ferrum documentation values</verify>
  <done>Button constants defined</done>
</task>

<task id="5">
  <name>Add right-click support (optional)</name>
  <files>core/ferrum_controller.py</files>
  <action>Add click_right(x: int, y: int) -> None method for future use: send km.move(x, y), then send km.click(BUTTON_RIGHT)</action>
  <verify>Method works if tested: sends km.click(1)</verify>
  <done>click_right() method implemented</done>
</task>

<task id="6">
  <name>Test mouse actions</name>
  <files>tests/test_ferrum_mouse.py</files>
  <action>Create simple test script: connect to Ferrum, call click(100, 100), call scroll("down", 3), verify commands sent correctly</action>
  <verify>Commands sent correctly to device (check logs or serial monitor)</verify>
  <done>Mouse actions tested successfully</done>
</task>

---

## Verification Criteria

- [ ] click() sends km.move then km.click(0)
- [ ] scroll() sends km.wheel with correct direction
- [ ] Button constants defined and correct
- [ ] All methods return without error
- [ ] Commands formatted per Ferrum spec

---

## Must-Haves for Goal Achievement

1. click(x, y) method implemented
2. scroll(direction, ticks) method implemented
3. Commands use correct Ferrum syntax
4. Method signatures match ActionDispatcher expectations

---

## Notes

- km.move is relative, not absolute positioning
- HardwareInputGateway adds jitter delays after actions
- Ferrum device handles click timing (75-125ms press, 125-175ms release)
- Scroll security: only use ±1 per Ferrum documentation recommendation
