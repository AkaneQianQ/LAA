# Plan 07.03: Keyboard Actions - HID Mapping and Key Press

**Phase:** 07-skills-ferrum
**Wave:** 1
**Depends On:** 07.01
**Estimated Duration:** 30-40 minutes

---

## Goal

Implement keyboard control for FerrumController with key name to HID code mapping,
supporting single keys and key combinations.

---

## Requirements Addressed

- REQ-KBD-01: Key name to HID code mapping
- REQ-KBD-02: Key press via km.down() + km.up()
- REQ-KBD-03: Key combination support (e.g., "alt+u")

---

## Implementation Details

### Ferrum KM API Reference

From documentation (Pages 36-40):

**km.down(key)** - Page 37
- Presses key indefinitely (until km.up() or hardware override)
- key: HID code (0-255)
- Use for hold scenarios or controlled press duration

**km.up(key)** - Page 37
- Releases key force press
- Release lasts 125-175ms (random), then returns to physical state

**km.press(key)** - Page 39
- Full click (press + release) with auto timing
- Press delay: 75-125ms (random)
- Release delay: 125-175ms (random)
- Returns to hardware state after

### HID Key Codes (HUT 1.5)

Common keys for Lost Ark automation:
```
ESC     = 41
A-Z     = 4-29
0-9     = 30-39
Enter   = 40
Space   = 44
Tab     = 43
Backspace = 42

Arrow Up    = 38
Arrow Down  = 40
Arrow Left  = 37
Arrow Right = 39

Left Alt    = 226
Right Alt   = 230
Left Ctrl   = 224
Right Ctrl  = 228
Left Shift  = 225
Right Shift = 229

F1-F12  = 58-69
```

### Key Name Format

Support these formats:
- Single keys: "u", "esc", "enter", "up"
- Key combinations: "alt+u", "ctrl+shift+a"

---

## Tasks

<task id="1">
  <name>Create HID key mapping table</name>
  <files>core/ferrum_controller.py</files>
  <action>Create KEY_MAP dictionary in ferrum_controller.py with: letters a-z (codes 4-29), numbers 0-9 (codes 30-39), special keys (enter=40, esc=41, backspace=42, tab=43, space=44), arrows (up=38, down=40, left=37, right=39), modifiers (alt=226, lalt=226, ralt=230, ctrl=224, lctrl=224, rctrl=228, shift=225, lshift=225, rshift=229), F keys f1-f12 (codes 58-69)</action>
  <verify>All required keys present: esc=41, u=24, enter=40, up=38, down=40, alt=226</verify>
  <done>KEY_MAP dictionary with all required keys</done>
</task>

<task id="2">
  <name>Implement key name parsing</name>
  <files>core/ferrum_controller.py</files>
  <action>Add _parse_key(key_name: str) -> List[int] method: split key_name by '+', convert each part to lowercase, look up in KEY_MAP, raise ValueError for unknown keys, return list of HID codes</action>
  <verify>Parsing works for single keys ("esc" -> [41]) and combinations ("alt+u" -> [226, 24])</verify>
  <done>_parse_key() method implemented</done>
</task>

<task id="3">
  <name>Implement press() method</name>
  <files>core/ferrum_controller.py</files>
  <action>Add press(key_name: str) -> None method: parse key_name to codes, if single code use km.press(code) for auto timing, if multiple codes press all modifiers first with km.down(), add 10ms delay between downs, hold for 50ms, release in reverse order with km.up()</action>
  <verify>Single keys use km.press, combinations use down/up sequence, "alt+u" presses alt first</verify>
  <done>press() method implemented with single key and combination support</done>
</task>

<task id="4">
  <name>Handle combo key ordering</name>
  <files>core/ferrum_controller.py</files>
  <action>Add _order_codes(codes: List[int]) -> List[int] helper: define modifiers set {224, 225, 226, 228, 229, 230}, separate modifiers from main keys, return modifiers first then main keys</action>
  <verify>"u+alt" still sends alt first after ordering, modifiers always precede main keys</verify>
  <done>_order_codes() ensures correct modifier-first ordering</done>
</task>

<task id="5">
  <name>Add hold/release methods (optional)</name>
  <files>core/ferrum_controller.py</files>
  <action>Add key_down(key_name: str) -> None method: parse key_name, send km.down(code) for each code. Add key_up(key_name: str) -> None method: parse key_name, send km.up(code) for each code</action>
  <verify>key_down("alt") sends km.down(226), key_up("alt") sends km.up(226)</verify>
  <done>key_down() and key_up() methods implemented for hold scenarios</done>
</task>

<task id="6">
  <name>Test keyboard actions</name>
  <files>tests/test_ferrum_keyboard.py</files>
  <action>Create test script: connect to Ferrum, test single keys (press("esc"), press("u"), press("enter")), test combination (press("alt+u")), verify correct HID codes sent</action>
  <verify>All keys send correct HID codes: esc=41, u=24, enter=40, alt+u sends 226 then 24</verify>
  <done>Keyboard actions tested successfully</done>
</task>

---

## Verification Criteria

- [ ] KEY_MAP contains all required keys
- [ ] press() handles single keys via km.press
- [ ] press() handles combinations via down/up sequence
- [ ] Key names parsed case-insensitively
- [ ] Combinations press modifiers first
- [ ] Unknown keys raise ValueError

---

## Must-Haves for Goal Achievement

1. KEY_MAP with common keys defined
2. press(key_name) method implemented
3. Key combination support (e.g., "alt+u")
4. Correct HID codes sent to Ferrum device

---

## Notes

- km.press is preferred for single keys (has built-in timing)
- km.down/up used for combinations (need controlled sequence)
- Hardware override applies to all keyboard commands
- Per Ferrum docs: releases last 125-175ms then return to physical state
