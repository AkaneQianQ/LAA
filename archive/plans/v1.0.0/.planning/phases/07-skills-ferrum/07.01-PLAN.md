# Plan 07.01: FerrumController Core and Serial Communication

**Phase:** 07-skills-ferrum
**Wave:** 1
**Depends On:** None
**Estimated Duration:** 30-45 minutes

---

## Goal

Create the FerrumController class with serial communication layer, implementing the base
interface expected by HardwareInputGateway. This plan establishes the foundation for all
Ferrum hardware interactions.

---

## Requirements Addressed

- REQ-HW-01: Ferrum serial communication via pyserial
- REQ-HW-02: Command/response protocol handling with echo parsing
- REQ-HW-03: Hardware initialization (km.init())

---

## Implementation Details

### Files to Create/Modify

1. **Create:** `core/ferrum_controller.py`
   - FerrumController class
   - Serial connection management
   - Command send/receive with echo handling
   - km.init() on connection

### FerrumController Interface Contract

The controller must implement these methods (as called by ActionDispatcher):
- `click(x: int, y: int) -> None`: Move and click left button
- `press(key_name: str) -> None`: Press a key by name
- `scroll(direction: str, ticks: int) -> None`: Scroll wheel
- `wait(seconds: float) -> None`: Sleep/delay

### Serial Protocol Details

From Ferrum documentation:
- Line terminator: `\r\n` for input, echoed as `\r\n` in output
- Prompt: `>>> ` (three arrows + space, no terminator)
- Command echo: Full command echoed back before result
- Example:
  ```
  Input:  km.init()\r\n
  Output: km.init()\r\n
          >>>
  ```

### Error Handling

- Serial timeout: 1 second default
- Connection failure: raise HardwareConnectionError
- Command timeout: retry once, then raise

---

## Tasks

<task id="1">
  <name>Create FerrumController class skeleton</name>
  <files>core/ferrum_controller.py</files>
  <action>Create core/ferrum_controller.py with: imports (serial, time, typing), FerrumController class definition, __init__ with port/baudrate/timeout parameters, _connect() method to establish serial connection, _disconnect() for cleanup</action>
  <verify>File exists with class structure and __init__ method accepting port, baudrate=115200, timeout=1</verify>
  <done>core/ferrum_controller.py created with FerrumController class skeleton</done>
</task>

<task id="2">
  <name>Implement command send/receive protocol</name>
  <files>core/ferrum_controller.py</files>
  <action>Add _send_command() method: format command with \r\n terminator, write to serial port, read response until ">>> " prompt, parse echoed command and result, return result string. Handle echo properly: read lines until prompt, first line(s) are echoed command, last line before prompt is result (may be empty)</action>
  <verify>Unit test with mock serial confirms correct parsing: send "km.init()" and verify response contains prompt</verify>
  <done>_send_command() implemented with echo handling and prompt parsing</done>
</task>

<task id="3">
  <name>Implement km.init() on connection</name>
  <files>core/ferrum_controller.py</files>
  <action>In __init__, after serial connection: send km.init() command, verify response (should return to prompt), log successful initialization with [Ferrum] prefix</action>
  <verify>Connection test shows km.init() sent successfully and "[Ferrum] 设备已初始化" or similar logged</verify>
  <done>km.init() sent on initialization, logged</done>
</task>

<task id="4">
  <name>Implement wait() method</name>
  <files>core/ferrum_controller.py</files>
  <action>Add wait(seconds: float) -> None method for ActionDispatcher compatibility: calls time.sleep(seconds)</action>
  <verify>Method exists and delays correctly: call wait(0.1) and verify ~100ms elapsed</verify>
  <done>wait() method implemented</done>
</task>

<task id="5">
  <name>Add error handling and connection validation</name>
  <files>core/ferrum_controller.py</files>
  <action>Add custom FerrumConnectionError exception, validate serial port is open before commands (raise if not), auto-reconnect on transient failures (retry once), context manager support (__enter__, __exit__)</action>
  <verify>Exceptions raised appropriately: test with closed port raises FerrumConnectionError, context manager works</verify>
  <done>Error handling and context manager implemented</done>
</task>

<task id="6">
  <name>Add logging</name>
  <files>core/ferrum_controller.py</files>
  <action>Log serial port open/close with [Ferrum] prefix, log all commands sent at DEBUG level, log responses at DEBUG level, log errors at ERROR level with [错误] prefix per project convention</action>
  <verify>Logs appear correctly in test runs: "[Ferrum] 串口已打开", "[Ferrum] 发送命令", "[错误]" on errors</verify>
  <done>Logging implemented with Chinese prefix tags</done>
</task>

---

## Verification Criteria

- [ ] FerrumController can connect to serial port
- [ ] km.init() sent on initialization
- [ ] Commands send correctly with \r\n terminator
- [ ] Echo responses parsed correctly
- [ ] Connection errors handled gracefully
- [ ] Context manager works for auto-cleanup

---

## Must-Haves for Goal Achievement

1. Serial connection to Ferrum device established
2. Command protocol correctly implemented (echo handling)
3. km.init() called on connection
4. wait() method available for ActionDispatcher

---

## Notes

- Serial port default: COM2 (consistent with existing project docs)
- Baudrate: 115200 (standard for Ferrum)
- Testing: Use loopback or mock serial if hardware unavailable
- Future plans will add click/press/scroll on top of this foundation
