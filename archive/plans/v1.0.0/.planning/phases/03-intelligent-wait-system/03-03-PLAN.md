---
phase: 03-intelligent-wait-system
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - config/workflows/guild_donation.yaml
  - tests/config_system/test_sleep_inventory_migration.py
  - tests/config_system/test_workflow_integration.py
autonomous: true
requirements:
  - WAIT-01
  - WAIT-02
  - WAIT-03
  - WAIT-04
  - SPEED-03
must_haves:
  truths:
    - "All 39 baseline hardcoded `time.sleep()` wait sites are migrated to state-driven waits in this phase (39/39 complete)."
    - "Production workflow configs use image-driven waits instead of fixed wait durations for UI transition synchronization."
    - "Workflow includes explicit appear and disappear waits where UI transitions require both directions."
    - "Automated inventory verification fails if new unmanaged hardcoded sleep sites are introduced."
    - "Integration tests prove config load -> compile -> execute path works with intelligent waits."
  artifacts:
    - path: "config/workflows/guild_donation.yaml"
      provides: "Migrated production workflow wait semantics using wait_image."
    - path: "tests/config_system/test_sleep_inventory_migration.py"
      provides: "Repo-wide sleep inventory assertion that enforces 39/39 baseline migration for SPEED-03."
    - path: "tests/config_system/test_workflow_integration.py"
      provides: "Integration assertions for intelligent wait actions and retry-on-timeout execution path."
  key_links:
    - from: "config/workflows/guild_donation.yaml"
      to: "core/config_loader.py"
      via: "YAML parse/validate/compile entrypoint"
      pattern: "wait_image|state|timeout_ms"
    - from: "tests/config_system/test_workflow_integration.py"
      to: "core.workflow_bootstrap.create_workflow_executor"
      via: "end-to-end bootstrap execution test"
      pattern: "create_workflow_executor"
    - from: "tests/config_system/test_sleep_inventory_migration.py"
      to: "repository source files"
      via: "inventory scan for hardcoded sleep calls"
      pattern: "time\\.sleep\\("
---

<objective>
Migrate wait behavior beyond the main workflow and add explicit proof that SPEED-03 (39 hardcoded sleep replacements) is satisfied.

Purpose: Remove fixed-delay dependency from runtime-configured automation paths and prevent regressions with a repo-wide inventory gate.
Output: Updated workflow config(s), sleep inventory verification, and integration tests aligned with intelligent wait contracts.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligent-wait-system/03-CONTEXT.md
@.planning/phases/03-intelligent-wait-system/03-RESEARCH.md
@config/workflows/guild_donation.yaml
@tests/config_system/test_workflow_integration.py
@.planning/phases/03-intelligent-wait-system/03-01-SUMMARY.md
@.planning/phases/03-intelligent-wait-system/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded workflow waits with state-driven wait_image coverage</name>
  <files>config/workflows/guild_donation.yaml</files>
  <behavior>
    - Test 1: Workflow YAML contains `wait_image` actions for transition-critical waits.
    - Test 2: At least one `state=appear` and one `state=disappear` are present.
    - Test 3: Remaining fixed wait actions from the 39-site baseline are fully removed from migrated automation flow.
    - Test 4: Wait timeout settings are configurable through defaults and selective step overrides.
  </behavior>
  <action>Refactor workflow-defined delays from `wait(duration_ms)`/hardcoded sleeps to explicit `wait_image` steps using template/roi/threshold/state semantics. Preserve deterministic step routing and existing retry fields while adding timeout/poll/retry interval controls per locked precedence model. Complete migration for all 39 baseline sleep sites within this phase; do not treat exclusions as success criteria.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_integration.py -k "guild_donation" -x</automated>
  </verify>
  <done>Production workflow YAML expresses wait behavior as UI-state transitions, with no unmanaged fixed-delay waits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add repo-wide hardcoded sleep inventory verification for SPEED-03</name>
  <files>tests/config_system/test_sleep_inventory_migration.py</files>
  <behavior>
    - Test 1: repository scan reports zero unmanaged `time.sleep()` sites in phase scope.
    - Test 2: inventory count tracks historical target (39) and requires migration for all 39 baseline sites.
    - Test 3: any newly introduced in-scope hardcoded sleep fails CI.
    - Test 4: exclusions are not allowed for baseline 39; newly discovered out-of-scope locations can be excluded only after explicit roadmap amendment.
  </behavior>
  <action>Create an automated inventory test that scans relevant Python/workflow sources for hardcoded sleep usage and enforces closure of the 39-site baseline. The gate must fail unless all 39 baseline sites are migrated to state-driven wait behavior (39/39). Do not allow exclusion manifests for baseline sites. Allow exclusions only for newly discovered out-of-scope files if roadmap scope is explicitly amended first, and require that amendment reference in test metadata.</action>
  <verify>
    <automated>pytest tests/config_system/test_sleep_inventory_migration.py -x</automated>
  </verify>
  <done>SPEED-03 has an executable proof gate that only passes when baseline migration is 39/39 and blocks partial migration claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update integration tests for intelligent wait execution path</name>
  <files>tests/config_system/test_workflow_integration.py</files>
  <behavior>
    - Test 1: bootstrap successfully loads and executes workflow containing wait_image.
    - Test 2: tests assert intelligent wait actions are present in compiled workflow.
    - Test 3: timeout failure in intelligent wait can be retried by executor path.
  </behavior>
  <action>Adjust integration tests so they validate intelligent wait adoption in the real workflow file and in temporary end-to-end fixtures. Remove assumptions that require legacy wait actions in core assertions, and add at least one retry-on-timeout integration scenario to preserve WAIT-04 traceability.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_integration.py -x</automated>
  </verify>
  <done>Integration coverage proves intelligent waits function through full config/bootstrap/executor wiring.</done>
</task>

</tasks>

<verification>
Run integration tests plus targeted config/executor tests, then run sleep inventory verification to prove all 39 baseline sleep sites are migrated (39/39).
</verification>

<success_criteria>
- All 39 baseline hardcoded `time.sleep()` sites are migrated in this phase (39/39), with no exclusion-as-success path.
- Migrated workflows no longer rely on unmanaged fixed-duration wait steps for UI transitions.
- Both appear and disappear intelligent waits are represented in config and executed through runtime.
- End-to-end tests and inventory gate cover intelligent wait loading, execution, retry behavior, and regression protection.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligent-wait-system/03-03-SUMMARY.md`
</output>
