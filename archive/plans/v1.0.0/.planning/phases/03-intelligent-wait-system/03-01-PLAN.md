---
phase: 03-intelligent-wait-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflow_schema.py
  - tests/config_system/test_config_loader_and_schema.py
autonomous: true
requirements:
  - WAIT-01
  - WAIT-02
  - WAIT-03
must_haves:
  truths:
    - "Workflow config accepts an explicit `wait_image` action with `state: appear|disappear`."
    - "Workflow config supports global wait defaults with per-step/per-action override semantics."
    - "Legacy `wait(duration_ms)` steps continue to validate for phased migration."
  artifacts:
    - path: "core/workflow_schema.py"
      provides: "WaitImage action contracts, wait defaults contracts, and step-level retry interval contract."
    - path: "tests/config_system/test_config_loader_and_schema.py"
      provides: "Schema validation tests for wait_image and backward compatibility."
  key_links:
    - from: "core/workflow_schema.py"
      to: "core/config_loader.py"
      via: "WorkflowConfig.model_validate during YAML load"
      pattern: "WorkflowConfig"
    - from: "tests/config_system/test_config_loader_and_schema.py"
      to: "core/workflow_schema.py"
      via: "model validation assertions"
      pattern: "wait_image|state|timeout_ms|retry_interval_ms"
---

<objective>
Define the configuration contracts for intelligent waits so executors can implement state-driven wait behavior without guessing field semantics.

Purpose: Lock data shape before runtime wiring.
Output: Validated schema for `wait_image`, timeout defaults, and retry interval overrides.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligent-wait-system/03-CONTEXT.md
@.planning/phases/03-intelligent-wait-system/03-RESEARCH.md
@core/workflow_schema.py
@tests/config_system/test_config_loader_and_schema.py

<interfaces>
From core/workflow_schema.py:
```python
class WaitAction(BaseModel):
    type: Literal["wait"]
    duration_ms: int

class WorkflowStep(BaseModel):
    step_id: str
    action: ActionConfig
    retry: int = 0
    condition: Optional[dict] = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add intelligent wait schema contracts and defaults</name>
  <files>core/workflow_schema.py</files>
  <behavior>
    - Test 1: `wait_image` accepts `state=appear` with required image condition fields.
    - Test 2: `wait_image` accepts `state=disappear` and rejects invalid state values.
    - Test 3: Workflow-level defaults exist for timeout/poll/retry interval, while per-action fields can override.
    - Test 4: Existing `wait(duration_ms)` action remains valid.
  </behavior>
  <action>Extend `ActionConfig` with a dedicated `WaitImageAction` model (not a mutation of `WaitAction`) using explicit `state: appear|disappear`. Add workflow-level wait defaults model for tiered timeout/poll/retry settings and a step-level `retry_interval_ms` override contract. Keep backward compatibility for existing `wait` actions to support incremental migration.</action>
  <verify>
    <automated>pytest tests/config_system/test_config_loader_and_schema.py -k "wait_image or retry_interval or wait_action" -x</automated>
  </verify>
  <done>`WorkflowConfig.model_validate()` accepts both legacy and intelligent wait schemas and enforces the locked `appear|disappear` semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema validation tests for locked wait decisions</name>
  <files>tests/config_system/test_config_loader_and_schema.py</files>
  <behavior>
    - Test 1: Missing required wait_image fields fail fast.
    - Test 2: Invalid override precedence inputs fail validation.
    - Test 3: Valid wait defaults + step override config validates successfully.
  </behavior>
  <action>Add focused tests asserting new `wait_image` constraints, explicit state behavior, timeout/poll/retry interval override fields, and continued validation of legacy `wait`. Keep tests deterministic and aligned with existing schema-test style.</action>
  <verify>
    <automated>pytest tests/config_system/test_config_loader_and_schema.py -x</automated>
  </verify>
  <done>Schema tests protect WAIT-01/02/03 contract surface and prevent regression on legacy wait compatibility.</done>
</task>

</tasks>

<verification>
Run schema test suite and confirm no validation regressions for existing action types.
</verification>

<success_criteria>
- `wait_image` is a first-class schema action with explicit `state` and image condition fields.
- Timeout/poll/retry interval defaults are representable globally and overridable at step/action level.
- Legacy `wait(duration_ms)` remains supported.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligent-wait-system/03-01-SUMMARY.md`
</output>

