---
phase: 06-6-f1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/interactive/test_overlay.py
  - tests/interactive/overlay.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "User sees a 600x80 pixel semi-transparent overlay at top-left"
    - "User can drag the overlay by the title bar"
    - "User can close the overlay with X button"
    - "Overlay stays on top of other windows"
  artifacts:
    - path: "tests/interactive/overlay.py"
      provides: "Overlay UI class with Tkinter"
      exports: ["TestOverlay"]
    - path: "tests/interactive/test_overlay.py"
      provides: "Unit tests for overlay functionality"
  key_links:
    - from: "TestOverlay"
      to: "Tkinter Toplevel"
      via: "wm_attributes('-topmost', True)"
---

<objective>
Create the core overlay UI system for the interactive test flow.

Purpose: Provide a semi-transparent horizontal overlay that displays test instructions and receives user feedback via hotkeys.
Output: Functional overlay module with 600x80 pixel UI, draggable title bar, close button, and always-on-top behavior.
</objective>

<execution_context>
@C:/Users/Akane/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-6-f1/06-CONTEXT.md
@gui_launcher.py

## Key Patterns from Existing Code

From gui_launcher.py:
- Use `tkinter` and `ttk` for UI components
- Use `keyboard` library for global hotkeys
- Use `threading.Event` for stop signals
- Use `wm_attributes('-topmost', True)` for always-on-top windows

## UI Specifications (from CONTEXT.md)

- **Size**: 600x80 pixels
- **Opacity**: 70% (alpha 0.7)
- **Theme**: Dark (dark gray/black background, white text)
- **Position**: Default top-left corner (0, 0)
- **Drag area**: 20px title bar at top
- **Close button**: X button on right side
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create overlay UI module</name>
  <files>tests/interactive/overlay.py</files>
  <action>
Create `tests/interactive/overlay.py` with a `TestOverlay` class:

1. **Class structure**:
   - `TestOverlay` class with `__init__(self, title="Test Flow")`
   - Create a Toplevel window (not Tk) so it can work with existing apps
   - Set geometry to "600x80+0+0" (top-left default)
   - Set `wm_attributes('-topmost', True)` for always-on-top
   - Set `wm_attributes('-alpha', 0.7)` for 70% opacity
   - Override `wm_attributes('-transparentcolor', ...)` if needed for true transparency

2. **Visual layout** (using tk.Frame, not ttk for bg color control):
   - Dark background: `#2b2b2b` or `#1e1e1e`
   - White text: `#ffffff`
   - Title bar (20px height): Shows "FerrumBot Test Flow" and drag handle
   - Main content area: 60px height for instruction display
   - Close button (X) on right side of title bar

3. **Drag functionality**:
   - Bind `<Button-1>` on title bar to start drag
   - Bind `<B1-Motion>` to move window
   - Store offset from window root to calculate new position

4. **Methods**:
   - `show()`: Display the overlay
   - `hide()`: Hide the overlay (withdraw)
   - `set_instruction(text: str)`: Update main content area text
   - `close()`: Destroy window and cleanup
   - `is_visible() -> bool`: Check if window exists

5. **Chinese UI text**:
   - Default title: "FerrumBot 测试控制"
   - Close button tooltip: "关闭"

Avoid:
- Using ttk.Frame (cannot set bg color easily)
- Making the window resizable
- Using grid layout (pack is simpler for this fixed-size UI)
  </action>
  <verify>
    <automated>cd C:/Users/Akane/FerrumProject/LostarkGuildDonationBot && python -c "from tests.interactive.overlay import TestOverlay; print('Import OK')"</automated>
  </verify>
  <done>TestOverlay class exists with all required methods, imports without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create overlay unit tests</name>
  <files>tests/interactive/test_overlay.py</files>
  <action>
Create `tests/interactive/test_overlay.py` with pytest tests:

1. **Test class**: `TestOverlayUI`

2. **Tests**:
   - `test_overlay_creation`: Verify TestOverlay instantiates without error
   - `test_overlay_geometry`: Verify default size is 600x80
   - `test_overlay_position`: Verify default position is (0, 0)
   - `test_overlay_always_on_top`: Verify topmost attribute is set
   - `test_overlay_opacity`: Verify alpha is approximately 0.7
   - `test_set_instruction`: Verify instruction text updates
   - `test_show_hide`: Verify show/hide methods work
   - `test_close_cleanup`: Verify close destroys window properly

3. **Use pytest fixtures**:
   - `overlay` fixture that creates and auto-cleans up TestOverlay
   - Use `request.addfinalizer` to ensure window is destroyed after each test

4. **Mock where needed**:
   - Mock `tk.Toplevel` methods that require display if running headless
   - Skip tests that require actual display with `@pytest.mark.skipif` checking `os.environ.get('DISPLAY')`

5. **Test structure**:
```python
import pytest
import tkinter as tk
from tests.interactive.overlay import TestOverlay

class TestOverlayUI:
    @pytest.fixture
    def overlay(self):
        root = tk.Tk()
        o = TestOverlay(root)
        yield o
        o.close()
        root.destroy()

    def test_overlay_creation(self, overlay):
        assert overlay.is_visible()
```
  </action>
  <verify>
    <automated>cd C:/Users/Akane/FerrumProject/LostarkGuildDonationBot && python -m pytest tests/interactive/test_overlay.py -v --tb=short 2>&1 | head -50</automated>
  </verify>
  <done>All overlay tests pass, coverage includes geometry, visibility, and methods</done>
</task>

<task type="auto">
  <name>Task 3: Add hotkey integration to overlay</name>
  <files>tests/interactive/overlay.py</files>
  <action>
Extend `TestOverlay` class with hotkey support:

1. **Hotkey registration** (using `keyboard` library, same as gui_launcher.py):
   - `register_hotkeys(self, callbacks: dict)` method
   - Callbacks dict format: `{"f1": on_f1, "end": on_end, "y": on_y, "n": on_n}`
   - Use `keyboard.add_hotkey()` for each key
   - Store hotkey handles for cleanup

2. **Callback signatures**:
   - All callbacks are `Callable[[], None]`
   - Overlay just calls them, doesn't implement logic

3. **Cleanup**:
   - `unregister_hotkeys(self)` method
   - Call from `close()` to prevent memory leaks
   - Use `keyboard.remove_hotkey()` for each registered handle

4. **Visual feedback for hotkeys**:
   - Add small indicator labels in overlay showing active hotkeys
   - F1: "下一步", END: "终止", Y: "通过", N: "失败"
   - Labels should be subtle (gray text) to not distract from instructions

5. **Error handling**:
   - Wrap hotkey registration in try/except
   - Log warning if keyboard library fails (e.g., permissions on Linux)
   - Don't crash if hotkeys can't be registered

6. **Test updates**:
   - Add `test_hotkey_registration` test
   - Add `test_hotkey_cleanup` test
   - Mock `keyboard` module for testing
  </action>
  <verify>
    <automated>cd C:/Users/Akane/FerrumProject/LostarkGuildDonationBot && python -m pytest tests/interactive/test_overlay.py -v -k "hotkey" --tb=short 2>&1</automated>
  </verify>
  <done>Hotkey registration and cleanup works, callbacks are called on key press</done>
</task>

</tasks>

<verification>
- [ ] TestOverlay can be imported and instantiated
- [ ] Overlay displays at 600x80 pixels, top-left by default
- [ ] Overlay is draggable via title bar
- [ ] Close button destroys the window
- [ ] Hotkeys (F1, END, Y, N) can be registered and trigger callbacks
- [ ] All unit tests pass
</verification>

<success_criteria>
1. `tests/interactive/overlay.py` exists with complete TestOverlay class
2. `tests/interactive/test_overlay.py` has passing tests for all functionality
3. Overlay can display instructions and receive hotkey input
4. Code follows patterns from gui_launcher.py (keyboard, threading)
</success_criteria>

<output>
After completion, create `.planning/phases/06-6-f1/06-01-SUMMARY.md`
</output>
