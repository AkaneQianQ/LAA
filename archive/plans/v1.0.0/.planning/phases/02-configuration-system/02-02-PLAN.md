---
phase: 02-configuration-system
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - core/workflow_executor.py
  - core/workflow_runtime.py
  - tests/config_system/test_workflow_executor.py
autonomous: true
requirements:
  - CFG-02
  - CFG-04
  - CFG-03
must_haves:
  truths:
    - "Configured workflows execute as deterministic multi-step sequences."
    - "Conditions branch execution using image detection results from the vision engine."
    - "Conditional loops are supported but guarded against runaway execution."
    - "Default step failure stops workflow unless per-step retry override is configured."
  artifacts:
    - path: "core/workflow_executor.py"
      provides: "Main step execution loop with explicit next resolution"
      exports: ["WorkflowExecutor"]
    - path: "core/workflow_runtime.py"
      provides: "Runtime adapters for action dispatch and condition evaluation"
      exports: ["ActionDispatcher", "ConditionEvaluator"]
    - path: "tests/config_system/test_workflow_executor.py"
      provides: "Executor, branching, retry, and loop safety tests"
  key_links:
    - from: "core/workflow_executor.py"
      to: "core/workflow_runtime.py"
      via: "dispatch and condition checks"
      pattern: "ActionDispatcher|ConditionEvaluator"
    - from: "core/workflow_runtime.py"
      to: "core/vision_engine.py"
      via: "image condition evaluation"
      pattern: "VisionEngine|find_element"
---

<objective>
Implement runtime orchestration for config-defined steps, including branching, retries, and condition-driven loops.

Purpose: Convert validated config into deterministic behavior matching locked execution semantics.
Output: Executor and runtime adapters with complete tests for action types and control flow.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-system/02-CONTEXT.md
@.planning/phases/02-configuration-system/02-RESEARCH.md
@core/workflow_schema.py
@core/workflow_compiler.py
@core/vision_engine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create executor tests and implement deterministic step traversal</name>
  <files>core/workflow_executor.py, tests/config_system/test_workflow_executor.py</files>
  <behavior>
    - Test: step sequence follows explicit `next_step_id` links.
    - Test: workflow terminates on terminal step without next link.
    - Test: default failure stops execution.
    - Test: retry override allows step retry before fail-stop.
  </behavior>
  <action>Write RED tests first for traversal and stop-on-failure semantics, then implement `WorkflowExecutor` with explicit cursor-based step progression and per-step retry policy handling.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_executor.py -x</automated>
  </verify>
  <done>Executor behavior is deterministic and aligned with stop-on-failure + retry override policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement action dispatcher and condition-based branching/loops</name>
  <files>core/workflow_runtime.py, core/workflow_executor.py, tests/config_system/test_workflow_executor.py</files>
  <behavior>
    - Test: click/wait/press/scroll actions invoke dispatcher with normalized parameters.
    - Test: image condition true routes to `on_true_step_id`; false routes to `on_false_step_id`.
    - Test: looped branches execute up to guard limit and fail safely when exceeded.
  </behavior>
  <action>Implement runtime adapters for four action types, including coordinate normalization for click (absolute + ROI-relative), wait in milliseconds, key_name passthrough for press, and direction+ticks for scroll. Evaluate conditions through vision engine image detection and route via explicit branch links. Add execution cap guard for conditional loops.</action>
  <verify>
    <automated>pytest tests/config_system/test_workflow_executor.py -x</automated>
  </verify>
  <done>All action types and conditional branching/loop semantics are executable from compiled config.</done>
</task>

</tasks>

<verification>
- Executor tests cover success path, failure path, retries, true/false branching, and loop guard behavior.
- Runtime uses existing vision primitives for condition checks, not a new detection engine.
</verification>

<success_criteria>
1. Multi-step workflows execute from config with explicit next-link behavior.
2. Conditional branches depend on image detection result.
3. Loops function through branch links with safety cap.
4. Action handlers support click/wait/press/scroll parameter contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-system/02-02-SUMMARY.md`
</output>
