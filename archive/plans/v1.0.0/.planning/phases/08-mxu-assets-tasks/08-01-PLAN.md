---
phase: 08-mxu-assets-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src-tauri/Cargo.toml
  - frontend/src-tauri/tauri.conf.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/interface.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/index.html
autonomous: true
requirements:
  - MXU-01
  - MXU-02
must_haves:
  truths:
    - "MXU frontend project structure exists with Tauri v2 + React 19 + TypeScript"
    - "Frontend can load and parse assets/interface.json configuration"
    - "Dark theme is default and Chinese (zh_cn) is default language"
    - "Frontend displays task list from interface.json"
  artifacts:
    - path: "frontend/package.json"
      provides: "Node.js dependencies and scripts"
      min_lines: 30
    - path: "frontend/src-tauri/tauri.conf.json"
      provides: "Tauri application configuration"
      exports: ["app", "build", "bundle"]
    - path: "frontend/src/App.tsx"
      provides: "Main React application component"
      min_lines: 50
    - path: "frontend/interface.json"
      provides: "Symlink/reference to assets/interface.json"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "assets/interface.json"
      via: "Tauri fs API readTextFile"
      pattern: "readTextFile.*interface\\.json"
---

<objective>
Set up MXU-based frontend project with Tauri v2, React 19, and TypeScript. Create the foundation for a graphical interface that integrates with FerrumBot's MaaEnd architecture.

Purpose: Provide a modern GUI framework that can parse interface.json and auto-generate task management UI.
Output: Complete frontend project structure with dark theme, Chinese language support, and interface.json loading capability.
</objective>

<execution_context>
@C:/Users/Akane/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-mxu-assets-tasks/08-CONTEXT.md
@assets/interface.json

## MXU Framework Reference
MXU (MaaFramework Next UI) is a Tauri v2 + React 19 + TypeScript GUI client.
Repository: https://github.com/MistEO/MXU

Key technologies:
- Tauri v2 (Rust backend for desktop apps)
- React 19 (UI framework)
- TypeScript (type safety)
- Vite (build tool)

## Existing Configuration
assets/interface.json already defines:
- Controllers: KMBox-Default (COM2), KMBox-Custom
- Resources: LostArk-KR-2560x1440
- Tasks: GuildDonation, CharacterSwitch, AccountIndexing
- Agent: Python service at agent/py_service/main.py

## UI Requirements from CONTEXT.md
- Dark theme as default
- Chinese (zh_cn) as default language
- Real-time log viewer
- Task-specific configuration panels
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Frontend Project Structure</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html</files>
  <action>
Create the frontend directory structure with package.json for a Tauri v2 + React 19 + TypeScript project.

1. Create frontend/ directory with subdirectories:
   - src/ (React TypeScript source)
   - src-tauri/ (Tauri Rust backend)
   - public/ (static assets)

2. Create package.json with dependencies:
   - react ^19.0.0
   - react-dom ^19.0.0
   - @tauri-apps/api ^2.0.0
   - @tauri-apps/plugin-shell ^2.0.0
   - @tauri-apps/plugin-fs ^2.0.0
   - Dev dependencies: @types/react, @types/react-dom, typescript, vite, @vitejs/plugin-react

3. Create vite.config.ts with Tauri plugin configuration
4. Create tsconfig.json with strict TypeScript settings
5. Create index.html with root div for React mounting

Use dark theme colors in index.html body background (#1a1a1a).
Set lang="zh-CN" on html element for Chinese default.
  </action>
  <verify>
    <automated>cd frontend && npm install --dry-run 2>&1 | head -20</automated>
  </verify>
  <done>
    - frontend/package.json exists with all required dependencies
    - frontend/vite.config.ts configured for Tauri
    - frontend/tsconfig.json has strict mode enabled
    - frontend/index.html has dark background and zh-CN lang
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tauri v2 Backend</name>
  <files>frontend/src-tauri/Cargo.toml, frontend/src-tauri/tauri.conf.json, frontend/src-tauri/src/main.rs, frontend/src-tauri/capabilities/default.json</files>
  <action>
Set up Tauri v2 Rust backend with necessary capabilities for FerrumBot integration.

1. Create src-tauri/Cargo.toml with:
   - tauri = { version = "2.0", features = [] }
   - tauri-plugin-shell = "2.0"
   - tauri-plugin-fs = "2.0"
   - tauri-plugin-websocket = "2.0" (for future WebSocket support)

2. Create src-tauri/tauri.conf.json with:
   - App name: "FerrumBot"
   - Window title: "FerrumBot - 失落的方舟自动化助手"
   - Window size: 1200x800 (reasonable for task configuration)
   - Dark theme by default
   - Permissions for fs:read-all (to read interface.json)

3. Create src-tauri/src/main.rs with basic Tauri setup and custom commands placeholder

4. Create src-tauri/capabilities/default.json with required permissions:
   - fs:allow-read-file (for interface.json)
   - fs:allow-read-dir (for assets directory)
   - shell:allow-execute (for spawning Python service)

5. Create src-tauri/.gitignore for Rust build artifacts
  </action>
  <verify>
    <automated>cd frontend/src-tauri && cargo check 2>&1 | grep -E "(Compiling|Finished|error)" | head -10</automated>
  </verify>
  <done>
    - src-tauri/Cargo.toml has all required dependencies
    - src-tauri/tauri.conf.json configured with dark theme
    - src-tauri/src/main.rs compiles without errors
    - Capabilities file grants necessary permissions
  </done>
</task>

<task type="auto">
  <name>Task 3: Create React UI Foundation with Interface Loading</name>
  <files>frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/types/interface.ts, frontend/src/hooks/useInterface.ts, frontend/interface.json</files>
  <action>
Create the React application foundation with interface.json loading capability.

1. Create frontend/src/types/interface.ts with TypeScript interfaces matching assets/interface.json structure:
   - InterfaceConfig (root config)
   - ControllerConfig (hardware controllers)
   - ResourceConfig (game resources)
   - TaskConfig (automation tasks)
   - AgentConfig (Python service)

2. Create frontend/src/hooks/useInterface.ts custom hook:
   - Uses @tauri-apps/plugin-fs to read interface.json
   - Parses JSON and returns typed InterfaceConfig
   - Handles errors gracefully with fallback
   - Returns loading state

3. Create frontend/src/main.tsx (React 19 entry):
   - Import React and ReactDOM
   - Import App component
   - Render with createRoot
   - Use StrictMode

4. Create frontend/src/App.tsx with:
   - Dark theme CSS (inline or imported)
   - useInterface hook to load config
   - Display app name and version from interface.json
   - List all tasks from interface.json with labels
   - Basic layout: header, task list sidebar, main content area
   - Chinese text for all UI labels

5. Create frontend/interface.json as a reference/symlink pointing to ../assets/interface.json

Styling requirements:
- Background: #1a1a1a (dark)
- Text: #e0e0e0 (light gray)
- Accent: #4a9eff (blue)
- Card backgrounds: #2a2a2a
- Use CSS Grid/Flexbox for layout
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - App.tsx displays FerrumBot name and version from interface.json
    - Task list shows all 3 tasks (GuildDonation, CharacterSwitch, AccountIndexing)
    - UI is in Chinese with dark theme
    - No TypeScript errors in build
  </done>
</task>

</tasks>

<verification>
1. Frontend directory structure matches MXU/Tauri conventions
2. package.json has all required dependencies for Tauri v2 + React 19
3. Tauri configuration has necessary permissions for file system access
4. React app successfully loads and parses assets/interface.json
5. UI displays in Chinese with dark theme by default
6. All TypeScript types match interface.json schema
</verification>

<success_criteria>
- Frontend project builds successfully (npm run build)
- Tauri backend compiles (cargo check in src-tauri)
- App displays FerrumBot configuration from interface.json
- Dark theme and Chinese language are defaults
- Task list is populated from interface.json tasks array
</success_criteria>

<output>
After completion, create `.planning/phases/08-mxu-assets-tasks/08-01-SUMMARY.md`
</output>
