---
phase: 04-error-recovery-xigncode3-compliance
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - core/hardware_input_gateway.py
  - core/compliance_guard.py
  - core/workflow_executor.py
  - core/workflow_bootstrap.py
  - tests/config_system/test_ace_compliance.py
autonomous: true
requirements:
  - ACE-01
  - ACE-02
  - ACE-03
  - ACE-04
must_haves:
  truths:
    - "All click/press/scroll actions execute through hardware input gateway only."
    - "Startup fails fast when hardware capability/policy checks do not pass."
    - "Input timing jitter is applied with bounded ±20% truncated-normal variance per session seed."
    - "Disallowed software injection, memory access, and process manipulation paths are blocked and audited."
  artifacts:
    - path: "core/hardware_input_gateway.py"
      provides: "Single ACE-compliant input egress and action auditing."
    - path: "core/compliance_guard.py"
      provides: "Startup policy checks and fail-fast enforcement."
    - path: "core/workflow_bootstrap.py"
      provides: "Guard invocation before executor startup."
    - path: "tests/config_system/test_ace_compliance.py"
      provides: "Behavior tests for policy enforcement and jitter bounds."
  key_links:
    - from: "core/workflow_executor.py"
      to: "core/hardware_input_gateway.py"
      via: "Action dispatch path for click/press/scroll"
      pattern: "gateway\\.(click|press|scroll)"
    - from: "core/workflow_bootstrap.py"
      to: "core/compliance_guard.py"
      via: "Initialization guard before runtime start"
      pattern: "validate|fail_fast"
    - from: "core/hardware_input_gateway.py"
      to: "core/error_logger.py"
      via: "Audit trail for blocked requests"
      pattern: "policy_violation|audit"
---

<objective>
Enforce ACE compliance by hardening input paths, startup checks, and timing policy so non-compliant behavior is technically impossible in normal execution.

Purpose: Convert compliance rules from documentation into enforceable runtime constraints.
Output: Hardware-only input gateway, fail-fast compliance guard, and bounded human-like timing jitter.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-CONTEXT.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-RESEARCH.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hardware-only input gateway with policy enforcement</name>
  <files>core/hardware_input_gateway.py, core/workflow_executor.py, tests/config_system/test_ace_compliance.py</files>
  <behavior>
    - click/press/scroll requests are routed through a single gateway abstraction.
    - Policy rejection raises input_policy_violation and writes audit event.
    - Executor no longer performs direct software input calls.
  </behavior>
  <action>Create HardwareInputGateway abstraction, inject it into executor dispatch path, and enforce explicit deny rules for non-hardware/suspicious input paths while preserving current action API expected by workflow steps.</action>
  <verify>
    <automated>pytest tests/config_system/test_ace_compliance.py -q -k "gateway or policy"</automated>
  </verify>
  <done>ACE-01 enforcement path is centralized and test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compliance guard fail-fast checks at startup</name>
  <files>core/compliance_guard.py, core/workflow_bootstrap.py, tests/config_system/test_ace_compliance.py</files>
  <behavior>
    - Bootstrap runs compliance validation before creating active executor session.
    - Missing hardware capability or forbidden configuration fails startup with explicit error.
    - Guard validates prohibition of memory/process injection feature flags or modules.
  </behavior>
  <action>Implement ComplianceGuard with handshake and policy validation hooks, wire it into create_workflow_executor startup path, and add tests for both pass and fail-fast scenarios.</action>
  <verify>
    <automated>pytest tests/config_system/test_ace_compliance.py -q -k "guard or fail_fast"</automated>
  </verify>
  <done>Non-compliant environments/configurations cannot start automation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply bounded timing jitter policy for hardware actions</name>
  <files>core/hardware_input_gateway.py, tests/config_system/test_ace_compliance.py</files>
  <behavior>
    - Jitter uses session-seeded truncated normal distribution.
    - Effective delay remains inside ±20% bounds for input actions.
    - wait_image polling cadence remains unchanged by jitter policy.
  </behavior>
  <action>Implement reusable jitter generator inside gateway for click/press/scroll delays, keep wait_image polling path unaffected, and add statistical boundary tests with fixed seed to verify determinism and limits.</action>
  <verify>
    <automated>pytest tests/config_system/test_ace_compliance.py -q -k "jitter or timing"</automated>
  </verify>
  <done>ACE-02 timing compliance is implemented without degrading wait_image logic.</done>
</task>

</tasks>

<verification>
Run ACE compliance tests, then run workflow executor regression tests to confirm input-path hardening does not break existing config execution behavior.
</verification>

<success_criteria>
1. Every runtime input action is hardware-gateway mediated.
2. Startup compliance checks fail fast on violations.
3. Timing jitter is bounded and reproducible with session seed.
4. No memory/process injection access path is available through runtime flow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-recovery-xigncode3-compliance/04-03-SUMMARY.md`
</output>
