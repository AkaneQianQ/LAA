---
phase: 04-error-recovery-xigncode3-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflow_schema.py
  - core/workflow_compiler.py
  - config/workflows/guild_donation.yaml
  - tests/config_system/test_error_recovery_schema.py
autonomous: true
requirements:
  - ERR-02
  - ERR-04
  - ACE-02
must_haves:
  truths:
    - "Workflow definitions can declare explicit recovery anchors and escalation limits."
    - "Compiler rejects invalid recovery references before runtime."
    - "Guild donation workflow includes explicit anchor metadata for recovery rollback paths."
  artifacts:
    - path: "core/workflow_schema.py"
      provides: "Recovery and audit schema fields with typed validation."
    - path: "core/workflow_compiler.py"
      provides: "Compile-time semantic checks for anchor reachability and rollback targets."
    - path: "config/workflows/guild_donation.yaml"
      provides: "Concrete anchor/recovery usage in production workflow."
    - path: "tests/config_system/test_error_recovery_schema.py"
      provides: "Schema and compiler validation coverage for recovery contracts."
  key_links:
    - from: "config/workflows/guild_donation.yaml"
      to: "core/workflow_schema.py"
      via: "Config loader model validation"
      pattern: "recovery|anchor"
    - from: "core/workflow_compiler.py"
      to: "core/workflow_schema.py"
      via: "Semantic validation over parsed StepConfig"
      pattern: "on_timeout|anchor|max_escalations"
---

<objective>
Define Phase 4 recovery contracts at schema/compiler level so runtime recovery logic can execute deterministically and fail fast on invalid configuration.

Purpose: Ensure ERR recovery behavior is configuration-driven and verifiable before runtime.
Output: Recovery-aware schema, compiler checks, and workflow anchor metadata.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-CONTEXT.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend workflow schema with explicit recovery and audit contracts</name>
  <files>core/workflow_schema.py, tests/config_system/test_error_recovery_schema.py</files>
  <behavior>
    - Parsing succeeds when a step defines recovery anchor fields and escalation limits.
    - Parsing fails when recovery fields have invalid types or illegal bounds.
    - Schema remains backward-compatible for existing Phase 1-3 workflows.
  </behavior>
  <action>Add pydantic models/fields for step-level recovery metadata (anchor marker, timeout rollback target, escalation cap, optional audit context) and add focused tests that pin acceptance and rejection cases without breaking current wait_image semantics.</action>
  <verify>
    <automated>pytest tests/config_system/test_error_recovery_schema.py -q</automated>
  </verify>
  <done>Recovery contract fields are typed, documented in schema, and covered by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compiler semantic validation for recovery graph safety</name>
  <files>core/workflow_compiler.py, tests/config_system/test_error_recovery_schema.py</files>
  <behavior>
    - Compiler rejects missing rollback target step ids.
    - Compiler rejects unreachable or cyclic recovery-only loops.
    - Compiler accepts valid anchor-linked recovery paths.
  </behavior>
  <action>Implement compile-time checks ensuring recovery targets exist, remain reachable, and do not create endless recovery loops; encode failure messages with step ids for debugging and keep normal route compilation behavior unchanged.</action>
  <verify>
    <automated>pytest tests/config_system/test_error_recovery_schema.py -q</automated>
  </verify>
  <done>Invalid recovery configurations fail at compile time with actionable errors.</done>
</task>

<task type="auto">
  <name>Task 3: Mark recovery anchors in guild donation workflow</name>
  <files>config/workflows/guild_donation.yaml, tests/config_system/test_guild_workflow_migration.py</files>
  <action>Annotate stable workflow points as explicit anchors and map timeout rollback branches to those anchors according to phase decisions; update existing workflow tests to assert anchor metadata is present and compilation still succeeds.</action>
  <verify>
    <automated>pytest tests/config_system/test_guild_workflow_migration.py -q</automated>
  </verify>
  <done>Guild workflow compiles with explicit anchor configuration and no regressions.</done>
</task>

</tasks>

<verification>
Run schema and workflow compilation suites to prove recovery contracts are enforceable before runtime and remain backward compatible.
</verification>

<success_criteria>
1. Recovery fields are available in schema and validated.
2. Compiler blocks unsafe recovery graph configurations.
3. Production workflow contains explicit anchor metadata and passes compilation tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-recovery-xigncode3-compliance/04-01-SUMMARY.md`
</output>

