---
phase: 04-error-recovery-xigncode3-compliance
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - core/workflow_executor.py
  - core/workflow_runtime.py
  - core/error_recovery.py
  - core/error_logger.py
  - tests/config_system/test_error_recovery_runtime.py
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
  - ERR-04
must_haves:
  truths:
    - "Timeout and lag failures escalate from step retry to rollback to role skip using deterministic thresholds."
    - "Disconnect scenarios are logged and current role is skipped without forced client restart."
    - "Every classified recovery failure is persisted as JSONL with required context fields."
  artifacts:
    - path: "core/error_recovery.py"
      provides: "Error taxonomy and escalation state machine."
    - path: "core/workflow_executor.py"
      provides: "Runtime integration of recovery orchestration and role-skip policy."
    - path: "core/error_logger.py"
      provides: "Structured JSONL logging and failure screenshot path handling."
    - path: "tests/config_system/test_error_recovery_runtime.py"
      provides: "Runtime and logging behavior tests for ERR requirements."
  key_links:
    - from: "core/workflow_executor.py"
      to: "core/error_recovery.py"
      via: "Exception classification and recovery decision"
      pattern: "ErrorKind|RecoveryOrchestrator"
    - from: "core/workflow_executor.py"
      to: "core/error_logger.py"
      via: "Structured failure event write"
      pattern: "log_error|jsonl"
    - from: "core/workflow_runtime.py"
      to: "core/workflow_executor.py"
      via: "Timeout propagation into retry/escalation"
      pattern: "Timeout|wait_image"
---

<objective>
Implement runtime recovery behavior and structured evidence logging for network lag, UI timeout, and disconnect scenarios.

Purpose: Deliver resilient execution that fails safely, recovers where possible, and leaves complete debug evidence.
Output: Recovery orchestrator, runtime integration, and JSONL logging with screenshot-on-failure policy.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-CONTEXT.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-RESEARCH.md
@.planning/phases/04-error-recovery-xigncode3-compliance/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement error taxonomy and escalation state machine</name>
  <files>core/error_recovery.py, tests/config_system/test_error_recovery_runtime.py</files>
  <behavior>
    - Error classifier maps runtime failures to network_lag/ui_timeout/disconnect/unknown kinds.
    - Escalation policy transitions L1 step retry -> L2 anchor rollback -> L3 role skip after configured threshold.
    - Repeated same-kind failures trigger circuit-break behavior for current role.
  </behavior>
  <action>Create a dedicated recovery module containing ErrorKind, ErrorContext, and RecoveryOrchestrator with deterministic counters and thresholds aligned to phase decisions (3 repeated failures before session-level escalation).</action>
  <verify>
    <automated>pytest tests/config_system/test_error_recovery_runtime.py -q -k "taxonomy or escalation"</automated>
  </verify>
  <done>Recovery orchestration behavior is unit-tested and deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire recovery decisions into executor/runtime flow</name>
  <files>core/workflow_executor.py, core/workflow_runtime.py, tests/config_system/test_error_recovery_runtime.py</files>
  <behavior>
    - wait_image timeout path routes through classifier and orchestrator.
    - Local rollback executes to explicit anchor when policy returns rollback action.
    - Disconnect classification results in role skip rather than forced restart.
  </behavior>
  <action>Refactor executor exception handling to centralize recovery routing, preserve existing step retry semantics, apply rollback jump actions only to valid anchor steps from compiled workflow, and terminate role execution safely when escalated to skip.</action>
  <verify>
    <automated>pytest tests/config_system/test_error_recovery_runtime.py -q -k "rollback or skip or timeout"</automated>
  </verify>
  <done>Executor performs policy-based recovery and role skip according to ERR-01/02/03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add structured JSONL error logging and failure evidence capture</name>
  <files>core/error_logger.py, core/workflow_executor.py, tests/config_system/test_error_recovery_runtime.py</files>
  <behavior>
    - Log records include ts, phase, step_id, error_kind, attempt, account, screenshot_path, context.
    - Screenshot path is populated only on failures where capture succeeds.
    - Console summary remains concise while full event is written to file.
  </behavior>
  <action>Implement JSONL logger with daily file partitioning under logs/errors, integrate it into recovery handling, and add tests asserting required fields and failure-only screenshot behavior.</action>
  <verify>
    <automated>pytest tests/config_system/test_error_recovery_runtime.py -q -k "logging or jsonl"</automated>
  </verify>
  <done>ERR-04 evidence pipeline is in place with validated record shape.</done>
</task>

</tasks>

<verification>
Run targeted runtime tests for classification, escalation, and logging, then run the broader config-system suite to ensure no regressions to prior phases.
</verification>

<success_criteria>
1. Runtime recovers deterministically from lag/timeout scenarios with configured escalation.
2. Disconnect events skip current role safely without restart automation.
3. Structured JSONL error logs capture complete context and failure evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-recovery-xigncode3-compliance/04-02-SUMMARY.md`
</output>

