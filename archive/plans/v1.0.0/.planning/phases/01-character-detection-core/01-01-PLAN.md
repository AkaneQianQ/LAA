---
phase: 01-character-detection-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/vision_engine.py
  - core/database.py
  - modules/character_detector.py
  - tests/character_detection/test_detector_contracts.py
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
must_haves:
  truths:
    - "系统可通过统一检测接口稳定判断 ESC 角色选择界面是否可用，并返回可消费的结构化结果。"
    - "系统对 9 个锁定 ROI 的占用检测在相同输入下输出一致，且阈值规则固定为 >= 0.8。"
    - "系统可持久化账号与角色截图元数据，并在重复运行时稳定读取同一账号缓存。"
  artifacts:
    - path: "core/database.py"
      provides: "SQLite schema initialization and repository APIs"
    - path: "modules/character_detector.py"
      provides: "CharacterDetector contracts and configuration constants"
    - path: "tests/character_detection/test_detector_contracts.py"
      provides: "Automated contract and schema tests"
  key_links:
    - from: "modules/character_detector.py"
      to: "core/vision_engine.py"
      via: "template-matching wrapper for masked ROI checks"
      pattern: "find_element|matchTemplate|TM_CCORR_NORMED|TM_CCOEFF_NORMED"
    - from: "modules/character_detector.py"
      to: "core/database.py"
      via: "repository initialization and persistence API"
      pattern: "init|save|upsert|account|character"
---

<objective>
Define the phase-1 implementation contracts and persistence foundation before behavior code.

Purpose: Remove ambiguity for execute agents and prevent schema/interface drift.
Output: Detector API contracts, OpenCV mask strategy, and SQLite schema with tests.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-character-detection-core/01-CONTEXT.md
@.planning/phases/01-character-detection-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create detector contracts and immutable ROI config</name>
  <files>modules/character_detector.py</files>
  <behavior>
    - Detector exposes APIs for ESC/menu detection, 9-slot occupancy scan, bottom detection, and full discovery loop.
    - Slot ROI constants exactly match the 9 locked coordinates from phase context.
    - Detection thresholds are centralized and default to slot >=0.8 with 3 retries.
  </behavior>
  <action>Implement `CharacterDetector` skeleton with typed return contracts and module constants for slot ROIs, scrollbar ROI, thresholds, and retry limits. Keep resolution fixed at 2560x1440 per scope. Enforce the locked phase decision by using `cv2.TM_CCOEFF_NORMED` with slot threshold `>= 0.8` for `CharacterISorNo.bmp`, and document FF00FF handling without reopening method selection.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_detector_contracts.py::test_detector_contract_surface -q</automated>
  </verify>
  <done>Detector API and config constants are importable, and the matching method/threshold strictly follow locked phase decisions (`TM_CCOEFF_NORMED`, `>=0.8`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SQLite schema and repository primitives for account/character cache</name>
  <files>core/database.py</files>
  <behavior>
    - Schema init can run multiple times without changing state unexpectedly.
    - Account rows include hash identity and created timestamp.
    - Character cache rows map account to slot_index and screenshot path.
  </behavior>
  <action>Create SQLite initialization and repository methods used by character discovery: create/find account by hash, upsert character screenshot metadata, and list cached characters by account. Use stdlib `sqlite3` with explicit timeout and short transactions.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_detector_contracts.py::test_database_schema_and_upsert -q</automated>
  </verify>
  <done>Database layer initializes and supports account + character metadata operations used by downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tests for contracts and schema bootstrap</name>
  <files>tests/character_detection/test_detector_contracts.py</files>
  <behavior>
    - Contract test fails if detector method signatures or config keys drift.
    - Schema test fails if required tables/columns are missing.
    - Upsert test fails if repeat writes create duplicates for same account/slot.
  </behavior>
  <action>Create focused pytest file that validates contract shape, ROI constants, and SQLite schema/repository behaviors against a temp test database.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_detector_contracts.py -q</automated>
  </verify>
  <done>Foundational contracts and persistence behavior are covered by fast automated tests.</done>
</task>

</tasks>

<verification>
Run detector contract tests and schema tests in one command to confirm foundation integrity.
</verification>

<success_criteria>
- Contract APIs exist for menu/slot/scroll discovery flows and can be imported without side effects.
- Locked slot and scrollbar ROIs are represented exactly in source constants.
- SQLite schema and upsert semantics pass automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-character-detection-core/01-01-SUMMARY.md`
</output>
