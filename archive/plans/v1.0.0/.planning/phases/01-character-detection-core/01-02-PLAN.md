---
phase: 01-character-detection-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - core/vision_engine.py
  - modules/character_detector.py
  - tests/character_detection/test_discovery_traversal.py
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
  - CHAR-03
must_haves:
  truths:
    - "System can verify ESC menu/character-selection state using template matching with retries."
    - "System evaluates all 9 visible slot ROIs and returns occupied/empty state deterministically."
    - "System traverses scroll pages and stops at detected scrollbar bottom to compute total character count."
  artifacts:
    - path: "modules/character_detector.py"
      provides: "Implemented discovery loop and slot detection behavior"
    - path: "tests/character_detection/test_discovery_traversal.py"
      provides: "Automated tests for slot scan and scroll termination"
  key_links:
    - from: "modules/character_detector.py"
      to: "assets/ESCMenuOrCharacterSelect.bmp"
      via: "ESC menu / character-selection readiness template matching"
      pattern: "esc|menu|character_selection|detect_character_selection_screen"
    - from: "modules/character_detector.py"
      to: "assets/CharacterISorNo.bmp"
      via: "slot occupancy template matching"
      pattern: "CharacterISorNo"
    - from: "modules/character_detector.py"
      to: "assets/Buttom.bmp"
      via: "scroll termination detection"
      pattern: "Buttom\\.bmp|bottom"
---

<objective>
Implement deterministic discovery behavior: menu validation, 9-slot scan, and full scroll traversal counting.

Purpose: Deliver core character discovery engine needed by all downstream automation.
Output: Working discovery loop with robust retries and test coverage.
</objective>

<execution_context>
@C:/Users/Akane/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Akane/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-character-detection-core/01-CONTEXT.md
@.planning/phases/01-character-detection-core/01-RESEARCH.md
@.planning/phases/01-character-detection-core/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ESC menu and character-selection detection workflow</name>
  <files>modules/character_detector.py, core/vision_engine.py</files>
  <behavior>
    - Detector attempts menu detection up to 3 retries with small jitter/backoff.
    - Success requires template confidence meeting configured threshold.
    - Failure returns structured false/error state without infinite loop.
  </behavior>
  <action>Implement `detect_character_selection_screen()` using existing vision capture/template APIs and retry policy from plan 01. Keep behavior deterministic and bounded.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_discovery_traversal.py::test_menu_detection_retry_and_failfast -q</automated>
  </verify>
  <done>Menu detection reliably reports ready/not-ready within bounded attempts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 9-slot occupancy scan using locked ROIs and masked template logic</name>
  <files>modules/character_detector.py</files>
  <behavior>
    - Exactly 9 ROIs are evaluated in row-major order.
    - Each slot returns occupied boolean and confidence score.
    - Detection uses FF00FF-aware matching strategy chosen in plan 01.
  </behavior>
  <action>Add `scan_visible_slots(frame)` that slices each locked ROI, runs template matching, and returns ordered results for row/column and slot index. Include retry-on-frame-refresh for borderline confidence values.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_discovery_traversal.py::test_scan_visible_slots_returns_nine_results -q</automated>
  </verify>
  <done>Visible slot scan returns stable occupied/empty results for all 9 slots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement scroll traversal and total-character counting with bottom termination</name>
  <files>modules/character_detector.py, tests/character_detection/test_discovery_traversal.py</files>
  <behavior>
    - Discovery loop scans page, accumulates count, scrolls by one row, and repeats.
    - Loop terminates when `Buttom.bmp` is detected in locked scrollbar ROI.
    - Duplicate rows are deduplicated so totals are not inflated.
  </behavior>
  <action>Implement `discover_total_characters()` integrating menu check, visible-slot scan, and bottom detection. Enforce locked context policy: scrollbar termination requires 100% pixel match for `Buttom.bmp` in the fixed scrollbar ROI (with bounded retries/confirmation). Add safeguards against infinite loops with max-page cap and meaningful error output.</action>
  <verify>
    <automated>python -m pytest tests/character_detection/test_discovery_traversal.py -q</automated>
  </verify>
  <done>Total character discovery works end-to-end, and loop termination is driven by exact 100% `Buttom.bmp` bottom match in the locked ROI without manual input.</done>
</task>

</tasks>

<verification>
Run traversal test suite to verify menu detection, slot scan integrity, and scroll-stop counting.
</verification>

<success_criteria>
- ESC/character-selection readiness check is automated and retry-bounded.
- Slot scan evaluates all locked ROIs and emits deterministic occupancy results.
- Total character count is discovered by scroll traversal with bottom-template termination.
</success_criteria>

<output>
After completion, create `.planning/phases/01-character-detection-core/01-02-SUMMARY.md`
</output>
